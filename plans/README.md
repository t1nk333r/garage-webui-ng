# Implementation Plans

Generated by the `improve` skill on 2026-07-24, against commit `ee420fb`.

> **License:** the contents of this `plans/` directory are released under the
> [MIT License](LICENSE), matching the upstream project (`LICENSE` at the repo
> root, © 2024 Khairul Hidayat).

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row in the table when done.

Plans 001-008 fix findings from the audit. Plans 009-011 are **design/spike**
plans for direction findings — their deliverable is a written recommendation in
`plans/design/`, not a merged feature.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| [001](001-stop-serving-cluster-secrets.md) | Stop serving Garage cluster secrets to the browser | P1 | S | LOW | — | **DONE** — branch `advisor/001-stop-serving-cluster-secrets`, unmerged |
| [002](002-verification-baseline.md) | Establish a verification baseline (tests, typecheck, CI) | P1 | M | LOW | — | **DONE** (1 revision) — branch `advisor/002-verification-baseline`, unmerged. CI lint is `continue-on-error`; confirmed by the maintainer 2026-07-24 |
| [003](003-paginate-listing-and-delete.md) | Paginate object listing and make recursive delete complete | P1 | M | MED | 002 | **DONE** — branch `advisor/003-paginate-listing-and-delete`, unmerged. Go verified; frontend Vitest test written but unrunnable until 002 merges alongside |
| [004](004-backend-correctness-cluster.md) | Fix backend correctness and resilience defects | P1 | M | MED | 002 | **DONE** — merged into `integration-check`; branch `advisor/004-backend-correctness-cluster` |
| [005](005-frontend-robustness.md) | Fix alias-less bucket crash, broken search, auth status contract | P2 | M | LOW | 002 | **DONE** (1 revision) — merged into `integration-check`. Plan step 6 corrected (GetStatus tests run through scs `LoadAndSave`). Branch `advisor/005-frontend-robustness` |
| [006](006-object-key-url-encoding.md) | Encode object keys in URLs and download filenames | P2 | M | MED | 002 | **DONE** — merged into `integration-check`; branch `advisor/006-object-key-url-encoding` |
| [007](007-session-hardening.md) | Harden session handling on the login path | P2 | S | LOW | — | **DONE** — branch `advisor/007-session-hardening` (commit `c620120`), unmerged |
| [008](008-docker-compose-and-docs-fixes.md) | Fix the Compose port mapping and the auth documentation | P2 | S | LOW | — | **DONE** — branch `advisor/008-docker-compose-and-docs-fixes`, unmerged |
| [009](009-design-presigned-share-links.md) | *(design)* Presigned share links for private buckets | P3 | M | LOW | — | **DONE + validated 2026-07-31** — verdict in [design/009](design/009-presigned-share-links.md): viable, needs a public-S3-endpoint config. Go-SDK path & read-only-key signing now verified end-to-end (both prior open questions closed) |
| [010](010-design-scoped-operator-role.md) | *(design)* A read-only operator role | P3 | M | LOW | 007 | **DONE** — verdict in [design/010](design/010-scoped-operator-role.md): viable, verb-based enforcement; client-heavy |
| [011](011-design-bulk-object-operations.md) | *(design)* Bulk object operations in the browser | P3 | M | LOW | **003** | **DONE** — verdict in [design/011](design/011-bulk-object-operations.md): build bulk-delete of explicit selection; skip select-all-across-pages |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with a one-line
reason) | `REJECTED` (with a one-line rationale — finding fixed independently or
approach abandoned)

**RELEASED as 1.2.0 (2026-07-30).** All 9 plans merged to `main` (`c5543b7`),
version-bumped, tagged `1.2.0`, pushed to fork `d7eeem/garage-webui` (main + tag;
upstream `origin` untouched). Frontend + prod binary (14.5 MB, UI embedded) build
clean. PRs: #1–#5 auto-merged, #6–#9 closed-as-landed (stacked on snapshot bases).

---

**All 8 fix plans are executed, reviewed, and merged into local `integration-check`
(builds green: 31 frontend tests, all Go pkgs under `-race`, prod binary). Pushed
to fork `d7eeem/garage-webui` as 8 stacked PRs (#1–#8), targeting the fork's own
`main` — never the public upstream. Local `main`, fork `main`, and `origin` remain
pristine at `ee420fb`.** Merge order: PRs #1–#5 (independent) → then retarget #6/#7
to `main` and merge → then #8. The `advisor/base-for-*` branches are PR-base
snapshots, deletable after the stack lands.

## Roadmap execution (direction findings → features, started 2026-08-03)

Post-1.2.0, the direction findings are being turned into features and executed in
this order (decisions locked: 009 gets `S3_PUBLIC_ENDPOINT_URL`; D3 = multi-user
local accounts; D4 = structured stdout logging). Each: author plan → dispatch
executor (reset to local `main`) → review + live-verify on local Garage → merge.

| Seq | Item | Plan | Status |
|---|---|---|---|
| 1 | D2a — abort orphaned multipart uploads | [013](013-abort-multipart-uploads.md) | **DONE** — live-verified (orphan→list→abort cycle on Garage), merged to `main` |
| 2 | 011 — bulk delete of selected objects | [014](014-bulk-object-delete.md) | **DONE** (1 rev) — live-verified endpoint (deleted 3/5; `errors:[]` fix) + Q4 all-errors fix, merged |
| 3 | 009 — presigned share links (build) | [015](015-presigned-share-links.md) | **DONE** — live-verified (no-auth fetch of presigned link → 200; disabled→501; 7d clamp), merged |
| 4 | D1 — cluster metrics panel | [016](016-cluster-metrics-panel.md) | **DONE** — live-verified (`/api/metrics` returns parsed Prometheus numbers; token stays server-side), recharts, merged |

**Scope (decided 2026-08-03):** executed the **core 4** (D1, D3, 010, D4), then
**D5** in a follow-up (maintainer said "execute D5"; scope "1 & 2"). **D2b (full
multipart upload) remains deferred, NOT dropped** — the maintainer will do it
later. D1 uses recharts (maintainer's choice); the panel is live/current-values
only (app is stateless — no historical time-series).
| 5 | D3 — multi-user local accounts | [017](017-multi-user-accounts.md) | **DONE** — live-verified (2 users log in, wrong pw/unknown → 401), merged |
| 6 | 010 — scoped operator role (build) | [018](018-scoped-operator-role.md) | **DONE** (1 rev) — `AUTH_VIEWER_USER_PASS`, fail-closed `isViewerAllowed`; live-verified (viewer→403 on writes/`showSecretKey`, 200 on reads; admin passes). Rev fixed `enabled` to OR both auth vars. Merged `e3354c0` |
| 7 | D4 — activity log (stdout) | [019](019-audit-log.md) | **DONE** — `AuditLog` middleware logs mutating requests (POST/PUT/DELETE) as JSON stdout lines; live-verified on Garage (admin write→`user:admin status:200`; viewer denied write→`status:403`; reads & login not logged; no `Session.Get` panic in `LoadAndSave` scope). Merged `9e0b833` |
| 8 | D2b — full browser multipart upload | — | pending (large) |
| 9 | D5 — static website hosting | [020](020-website-hosting-urls.md) | **DONE** — website *config* already existed; this fixed the broken/duplicated public-URL logic. Shared `src/lib/website.ts` helper (15 unit tests) used by overview + Share dialog; `[s3_web]`-not-configured guidance; bucket-list Website badge. Frontend-only. **Live-verified in-browser** on Garage: unconfigured→guidance (no broken URLs); configured→`http://mpu-test.web.local:3902` (overview) + `…/obj4.txt` (share); badge shows on website buckets. Merged `07e77d8` |

### Standalone plans (outside the roadmap)

| Plan | Title | Status |
|---|---|---|
| [021](021-password-hash-generator.md) | In-app client-side bcrypt password hash generator | **SUPERSEDED by 022–026** — its only purpose was producing `AUTH_USER_PASS` values; with users managed in-app nobody generates a hash by hand. Kept for history; **do not execute**. |

### Object browser

| Plan | Title | Status |
|---|---|---|
| [031](031-download-selected-objects.md) | Download selected objects as a streamed ZIP | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/031` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a2f1de2d91032384a`, branch `advisor/031-download-selected`, commit `ece5578`, **stacked on `advisor/035-…`**. **Reviewer re-verified** on pinned `golang:1.25.12`: gofmt clean, vet 0, `CGO_ENABLED=0` build 0, `go test -race ./...` all packages `ok`; frontend typecheck/test/build 0 (74 passed). Scope exactly the 7 in-scope files, and `git diff` of `browse.go` has **zero deletion lines** — provably additive, so `BulkDeleteObjects`, `PutObject`, `maxUploadBytes` and `drainRequestBody` are all untouched. **The two security guarantees were mutation-tested, not just read**: widening the viewer carve-out from an exact match to `strings.HasPrefix(path, "/browse/")` fails `posting_to_a_bucket_(delete)_is_still_denied`; deleting the single-use invalidation line fails `a_used_token_cannot_be_replayed` (403 instead of 404). Verified independently that `utils.Cache.Get` really does treat the executor's negative-TTL overwrite as expired (`cache.go` deletes when `expiresAt.Before(now)`) — the single-use property rests on that. Auth is checked **before** the first byte; single-use burns the token regardless of the bucket/user verdict (fail-closed). Live Garage check **not** performed. (stamped `bcbf86c`) — the selection toolbar offers only *Delete selected*; there is no way to get several objects out. Design forced by three measured constraints: a native download can't send the CSRF header, 1000 keys don't fit in a URL, and `fetch`+`blob` would buffer the archive in tab memory. Hence **POST mints a 60 s single-use token → GET streams the ZIP**. Token is bound to bucket **and** username. Mint lives on its own route (`POST /browse/download-token`) so the viewer carve-out cannot leak the delete action on `POST /browse/{bucket}`. Objects only — folders are not selectable. |
| [042](042-object-table-column-mismatch.md) | Fix the object table's column mismatch — the actions column is unreachable | **P1 · S · LOW · no deps.** **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/042` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-ab4d954430bb5e437`, branch `advisor/042-object-table-column-mismatch`, commit `cdd00f1`, from `main` `d9fab74`. **Reviewer re-verified**: typecheck 0, 14 files / **148 tests** / 1 pre-existing skip, build 0. Scope exactly the 2 in-scope files; the out-of-scope diff (`layouts/`, `manage/page.tsx`, `object-actions.tsx`, `tailwind.config.js`) is **empty**, so the layout's `overflow-hidden` was left alone as required. All four greps hit their exact targets: `colSpan={5}`→4, `colSpan={4}`→0, `colSpan={2}`→1, `max-w-[40vw]`→0. **Mutation-tested both defects**: removing the `sr-only` Actions header fails **all 4** tests; reverting the empty-state `colSpan` to 4 fails exactly the empty-state test. The tests compute the header count *dynamically* and compare it to each row's effective cell count summed over `colSpan` — a relative assertion that cannot be satisfied by hardcoding 5, which is what makes it a real regression guard. Judgment call accepted: a real `QueryClientProvider` is used rather than mocked, because `object-actions.tsx` calls `useQueryClient()` directly. Load-more row test skipped per the plan's own "skip if not feasible without excessive setup, and say so". **Visual check NOT performed** — jsdom cannot verify it and no browser was available. (stamped `d9fab74`, v3.4.0). Reported from a screenshot: the object table clips on the right — `Last Modified` truncates mid-word and the per-row actions column is off-screen entirely, so **Download, Share and Delete are unreachable**. Shipped in v3.4.0. Three defects compound, all in `object-list.tsx`. **(1)** `react-daisyui`'s `Table.Head` renders **one `<th>` per child** — verified by reading `react-daisyui.esm.js`, not assumed — and the head passes 4 children while every row emits 5 cells (`<ObjectActions>` contributes its own `<td>` at `object-actions.tsx:54`). The browser must size a 5th column with no header. **(2)** `colSpan` is `4` for loading/error/empty but `5` for load-more — one is wrong either way, and the disagreement is evidence the column count was never settled. **(3)** `max-w-[40vw]` on the filename is **viewport**-relative inside a sidebar-offset `.container`, so on a wide screen a long name pushes the table past its container; the reported bucket's 90-char `Balance Sheet_…` name is exactly that case. The wrapper's `overflow-x-auto` does not save it because `main-layout.tsx` sets `overflow-hidden` above it — but the plan **forbids** touching that: widening the layout hides the symptom and affects every route. Fix is three lines plus `min-w-0`/`max-w-0`, the idiom that makes `truncate` work in a table cell. **Testing note the plan makes explicit**: jsdom has no layout engine, so "is the column visible" is untestable and must not be faked by stubbing `getBoundingClientRect`; what *is* testable — and is precisely the defect — is that header cell count equals each row's effective cell count summed over `colSpan`. |
| [045](045-in-app-media-viewer-and-sortable-columns.md) | Preview media inside the console, and sort the object table | **DONE — MERGED & SHIPPED in v3.5.0** (`ecb9b1e`). Sonnet executor, branch `advisor/045-media-viewer-and-sorting`, commits `7412c99`+`b90b27d`, from `main` `90a91fd`. **Reviewer re-verified independently**: typecheck 0, 17 files / **177 tests** / 1 pre-existing skip (+26), build 0; Go untouched and all 6 packages still `ok` on pinned `golang:1.25.12`. Scope exactly the 7 in-scope files; `backend/` diff **empty**; `types.ts` diff **empty**; `dangerouslySetInnerHTML`/`<embed>`/`<object>`/`Z_LAYERS` greps all clean; `Table.Head` confirmed at exactly 5 children. **Reviewer ran 4 mutations directly** (not trusting the report): SVG→`"image"` fails the security-boundary test; in-place sort fails the purity test; removing the `NaN`→0 mapping fails the unparsable-date test; a 6th `Table.Head` child fails **4** tests including the plan-042 invariant re-asserted in the sorted state. **⚠️ ADVISOR ERROR — done-criterion 2 was unfalsifiable.** The plan demanded that dropping the null-guard on `size` fail a test. It cannot: JS coerces `null` in arithmetic, so `null - 100 === 0 - 100` exactly, and Go's `*int64` without `omitempty` always emits `null` and never omits the key — so no reachable data shape distinguishes guarded from unguarded. Executor proved it empirically, kept the guard as documentation of the pointer trap, and **added a test with real teeth instead** (an unparsable date string, which exercises the `Number.isNaN` branch the null-only fixtures short-circuited past). Correct call on both counts. **Manual browser checks NOT performed** — the reviewer's outstanding list: the five media types previewing in-modal, `.svg`/`.zip` still downloading, the mislabelled-`.png` fallback, prev/next order after a re-sort, and **specifically whether `Content-Security-Policy: sandbox` blanks the PDF `<iframe>` in Chrome/Firefox** (if it does, that is a plan decision, not a header relaxation). (planned at `90a91fd`, requested 2026-08-10). Two features in **one** plan on purpose: both edit `object-list.tsx`, so two executors would conflict on the same hunks. **Part A — in-app viewer.** Today `object-list.tsx:82` does `window.open(...?view=1)`, so every preview leaves the console. Replaced by a `createDisclosure` modal (the `share-dialog.tsx` pattern) rendering image/video/audio/pdf/text. Investigation established the viewer needs **no backend change**: object URLs are same-origin, so `img-src 'self'` covers `<img>` and `media`/`frame` fall back to `default-src 'self'`; `object-src 'none'` is why the PDF must use `<iframe>`, not `<embed>`; and 043's per-object `Content-Security-Policy: sandbox` is ignored for subresource loads (it applies to documents/workers) while still sandboxing the PDF frame — which is the wanted behaviour. **The asymmetry the plan is built around**: the backend decides inline-safety from the object's *stored* content type (`inlineSafeContentTypes`), the frontend can only guess from the extension — so `classifyMedia` is explicitly a *mirror, not a source of truth*, and every renderer carries an `onError` download fallback for when they disagree. **SVG stays excluded on both sides** (XML that can carry `<script>`, would execute on the console's origin) and is a tested security boundary, not a preference. Text is fetched and rendered as a React child with a 256 KB cap — `dangerouslySetInnerHTML` is a STOP condition. **Part B — sortable columns.** Name/Size/Last Modified, client-side. **Two traps the Go schema plants**: `BrowserObject.LastModified` is `*time.Time`, so it arrives as a **string** despite `types.ts` declaring `Date` (`.getTime()` throws), and both `Size` and `LastModified` are pointers so both can be `null` — and a `NaN` returned from a comparator silently corrupts the sort. Both get dedicated regression tests. Sorting covers **loaded pages only** — `ListObjectsV2` has no server-side sort — so a hint renders when a non-default sort is active with more pages pending; fetching all pages to fix it is a STOP condition. The header must keep **exactly five children** (the plan-042 `Table.Head` trap), re-asserted in the sorted state. Five mutation tests required. |
| [046](046-pdf-preview-framing-hotfix.md) | PDF previews are blank — `X-Frame-Options: DENY` on object responses | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/046` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, branch `advisor/046-pdf-preview-framing-hotfix`, commit `b59b7ef`, from `main` `9ed7f65` (v3.5.0). **P1 shipped regression**: v3.5.0's notes advertise PDF preview and it was broken for every PDF. **Diagnosed from measured response headers, not inference** — a live capture showed `content-type: application/pdf`, **no** `Content-Disposition` (so `isInlineSafe` was working correctly and the body was offered inline) and `x-frame-options: DENY`. `DENY` forbids framing by *anyone*, same-origin included, so the viewer's `<iframe>` could never paint; images/video/audio were unaffected because they are not frames, which matched the symptom exactly. Root cause is a **split-brain between two files**: `middleware/headers.go` sets `X-Frame-Options` globally, the object handler overrode only the CSP, and `grep -c "X-Frame-Options" backend/router/browse.go` was **0**. Fix overrides both headers **on the view path only** — `SAMEORIGIN` plus `frame-ancestors 'self'` — leaving the global `DENY` intact for the console's own HTML (narrowing it globally would reopen the clickjacking hole 043 closed). `allow-scripts` is granted **for PDFs only** (browsers render PDFs with a scripted viewer; a fully sandboxed frame blanks it) and **`allow-same-origin` is never granted**, so the body keeps an opaque origin and still cannot read the `csrf_token`, touch the parent DOM, or make credentialed requests. **Reviewer re-verified**: typecheck 0, 17 files / **180 tests** / 1 pre-existing skip, build 0; Go gofmt/vet clean and all 6 packages `ok`. Scope exactly 4 files; `middleware/headers.go` diff **empty**; `inlineSafeContentTypes` byte-for-byte unchanged; `allow-same-origin` appears in `browse.go` exactly once — in a comment stating it is *not* granted. **Reviewer ran both security mutations directly**: granting `allow-same-origin` fails the explicit substring guard; dropping `frame-ancestors` fails the policy test. Also fixes a second defect: the PDF branch was the only one with no error path, and **`<iframe>` cannot fire `onError`** for a response the browser declines to render — so a failure was a permanent blank box. Replaced with an always-present *Open in new tab / Download* affordance rather than an unreliable detection heuristic. **⚠️ ADVISOR ERROR — self-contradictory done-criterion.** The plan demanded `grep -c "allow-same-origin" backend/` → **0** while Step 2 simultaneously required an explicit `strings.Contains(..., "allow-same-origin") == false` guard, which necessarily puts the literal in the test file. Satisfying the grep would have meant deleting the very regression test the plan called essential. Executor resolved in favour of the test and flagged it — correct. Same failure mode already recorded for 029/030/034/035/036/041. **VERIFIED IN A BROWSER on v3.5.1 (2026-08-11)** — a 26-page PDF renders inside the viewer with the full Chrome PDF UI (page nav, zoom, thumbnail rail, print/download), and the *Open in new tab / Download* escape hatch renders beneath it as specified. **This resolves the plan's one residual uncertainty**: that `sandbox allow-scripts` (without `allow-same-origin`) suffices for Chrome's PDF viewer was *reasoned, not measured* when shipped — it is now measured. The opaque origin is retained, so the 043 property still holds. Still unverified: Firefox specifically, and that a non-PDF inline type still receives plain `sandbox` with no `allow-scripts`. |
| [047](047-generic-file-type-icons.md) | Replace row thumbnails with generic per-type icons | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/047` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, branch `advisor/047-generic-file-type-icons`, commit `bae7f59`, from `main` `4b3d529`. Maintainer's correction: *"its not icons its thumbnails i did not ask for thumbnails just make it generic thumbnail for media"* — so real thumbnails are **deleted**, not repaired, and three defects die with the `<img>` path: **(1)** non-`jpg/png/gif` images fetched the **full original** to draw a 20px row icon (the backend registers only gif/jpeg/png decoders in `utils/image.go`, so `webp`/`avif`/`bmp`/`ico` had no thumbnail path at all — a 10 MB `.webp` cost 10 MB per row); **(2)** `.svg` row icons broke in v3.5.0 because 043 serves SVG as an attachment, so the `<img>` failed; **(3)** every non-image type collapsed to one generic document glyph. A fourth, unexplained symptom (two `.pdf` rows rendering a collapsed `<img>`-like mark while a third rendered normally) was **deliberately not investigated** — the plan deletes the code path, so it cannot survive whatever its cause was. Extension-driven `Record` lookup, **not** `mime.getType` — `mime/lite` does not know `.ico` and has gaps for `.avif`/`.heic`, and the old `mime.getType(ext)?.split("/")[0]` shape is what made the component fragile. Ten groups: clapperboard/image/audio/pdf/spreadsheet/presentation/archive/code/text/default. **Reviewer re-verified**: typecheck 0, 18 files / **198 tests** / 1 pre-existing skip (+18), build 0. Scope exactly 4 files; `backend/`, `media-viewer.tsx` and `sorting.ts` diffs all **empty**; `Table.Head` untouched (no header hunks in the diff); `grep img object-list.tsx` → 0, `grep thumb=1 src/` → 0, `grep mime file-icons.ts` → 0. **Reviewer ran all three mutations directly**: unconditional default icon fails 13 cases; dropping `.toLowerCase()` fails exactly the case-insensitivity test; restoring the `<img>` branch fails exactly the regression guard. **Design boundary held**: the icon map is a *separate list* from `classifyMedia`, which mirrors the backend security allowlist — so `.svg` shows an image icon and still downloads when clicked. Merging them would let a cosmetic edit widen a security boundary; both files carry a comment saying so and it is a STOP condition. **Open question for the maintainer**: the brief said *"image will have a film strip"*, but a film strip conventionally denotes video, so video got the clapperboard and images a picture glyph. One-line swap if meant literally. **Manual browser check NOT performed** — outstanding: type-appropriate icons per row, and **no per-row network request on folder load** (the actual performance win). Leaves the backend `?thumb=1` endpoint with no UI caller; kept deliberately, removal is a separate decision. |
| [048](048-archive-entry-name-traversal.md) | Sanitise ZIP entry names — the archive can carry `../` out of the extraction directory | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/048` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, branch `advisor/048-archive-entry-name-traversal`, commit `5b02815`, from `3c7b69b` (v3.5.1). **Reviewer re-verified**: gofmt/vet/build clean, `go test -race ./...` all 6 packages `ok`. Scope exactly the 2 in-scope files; `src/`, `.github/`, `backend/middleware/` diffs **empty**; `TestStripCommonKeyPrefix` unmodified (0 removed lines); `zw.Create(entryNames[key])` still exactly one match. **Reviewer ran two mutations directly** — substring-match instead of exact `==` turns `...hidden.txt` into `""` (the filename is *erased*, not merely mis-handled), and a pass-through sanitiser fails on `../../etc/passwd` and `/etc/passwd`. **⚠️ EXECUTOR TERMINATED MID-MUTATION on an API session limit, with mutation 1 still applied and nothing committed** — `safeZipEntryName`'s body was destroyed and had to be rewritten from the (intact) tests on resume. **Process lesson, now folded into the plan template**: mutation testing must copy the file first and commit *before* mutating; a crash inside the mutation window destroys uncommitted work. This is the second such loss this session. Everything else survived, and because the 12-case test was already written it served as an exact spec for the rewrite. **⚠️ ADVISOR ERROR — the plan was internally inconsistent, caught by the executor.** §1 and Step 4 claimed `a/../x.txt` and `x.txt` both sanitise to `x.txt` and therefore collide. Under the discard-only algorithm the plan *itself* specifies (and pins with the `a/../../b.txt` → `a/b.txt` row), `a/../x.txt` becomes `a/x.txt` and does **not** collide — the example assumed *resolve* semantics while the rule mandated *discard*. Executor substituted `../x.txt`, which genuinely collides, preserving the test's intent without bending the algorithm. Correct call. A second, trivial slip: the done-criterion `grep -c stripCommonKeyPrefix` → 2 is actually 3 in both HEAD and the baseline (the function's own doc-comment line). **Manual live-cluster check NOT performed** — outstanding: store an object whose key contains `../` via the S3 API directly, download the archive, confirm `unzip -l` shows no `..` and that `RENAMED-ENTRIES.txt` names the key. (planned at `3c7b69b`). First of the six unplanned audit findings. **Confirmed by reading the code, not inferred**: `DownloadArchive` does `zw.Create(entryNames[key])` where names come from `stripCommonKeyPrefix(dl.Keys)`, which only trims a shared prefix — and `grep -rn "\.\./|filepath.Clean|path.Clean|IsAbs" backend/router/*.go` (excluding tests) returns **nothing**, so no validation exists anywhere. **The inversion that makes it real**: this is not zip-slip-on-extract, it is **zip-slip-on-produce** — S3 keys are arbitrary UTF-8, anyone with S3 write access to the bucket (**not necessarily a console user** — Garage's S3 API is reachable independently) can store `../../../.ssh/authorized_keys`, and the console builds that name verbatim into an archive handed to its own operator. Some extractors strip it; relying on the victim's choice of tool is not a control. Second path: `CreateDownloadToken` takes `Keys` verbatim from request JSON with no validation. Third, related defect: **nothing prevents two keys producing the same entry name** — a zip with duplicate entries extracts by overwriting, an overwrite primitive plus plain data loss, so de-duplication is specified as part of the security property rather than tidiness. Fix adds `safeZipEntryName` (backslash→slash, drop drive-letter/UNC, discard `""`/`.`/`..` segments) and `archiveEntryNames` (strip → sanitise → dedupe, deterministic in key order), leaving `stripCommonKeyPrefix` and its four tests untouched. **Sanitise, never reject** — a `../` key can be a real object the operator is entitled to download, and `DownloadArchive` commits its HTTP status before the first entry so late failure has nowhere to report; renames surface in a `RENAMED-ENTRIES.txt` note beside the existing `DOWNLOAD-ERRORS.txt`. **Two traps written into the plan**: `path.Clean("../a") == "../a"`, so Clean is explicitly *not* the mechanism; and only an **exact** `..` segment is dangerous — a `strings.Contains(name, "..")` check would corrupt `...hidden.txt` and `..bashrc`, so there is a test row and a mutation for it. End-to-end handler testing is **not** feasible (past the token checks `DownloadArchive` needs a live `getS3Client`), so the plan tests the pure functions exhaustively and says so rather than having an executor build an S3 fixture. |
| [049](049-release-integrity-signing.md) | Sign releases — checksums + ed25519 signature | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/049` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge + maintainer keygen.** Sonnet executor, branch `advisor/049-release-integrity-signing`, commit `0009e73`, from `main` `9858186`. **Reviewer re-verified**: gofmt/vet/build clean, `go test -race ./...` **all 7 packages `ok`** (incl. the new `cmd/relsign`); YAML parses; scope exactly the 6 in-scope files; `backend/version.go`, `backend/router/`, `src/`, `Dockerfile`, `ci.yml` diffs **empty**. **Key-material audit passed**: `git grep -nE "[0-9a-f]{64,}"` over `backend/` and `.github/` outside tests → **no matches**; `releasePublicKey = ""` as required; working tree clean. `runSign` reads the key **only** via `os.LookupEnv` — there is no `-key` flag defined at all, and the decode error deliberately omits the value, its length and any prefix. **Workflow structure confirmed correct by parsing the YAML**, not by reading it: `binaries` now *only* uploads artifacts (no `action-gh-release` step remains in it) and `release` `needs: binaries` is the sole publisher — which is the whole point, since no matrix job ever sees both binaries. **Reviewer ran the two decisive mutations**: neutering `ed25519.Verify` fails all three tamper tests (tampered payload, tampered signature, wrong key) while the round-trip still passes — exactly the asymmetry that proves the tests test verification rather than plumbing; hardcoding `ReleasePublicKey()` fails the accessor test. *(A first attempt at the verify mutation failed to **build** rather than fail a test, which proves nothing; re-run in a form that compiles.)* **Executor behaved correctly on the security constraint**: it attempted one manual `relsign keygen` sanity check, was blocked by the environment's own safety classifier, and **did not work around it** — substituting an in-process Go test that captures keygen output into `bytes.Buffer` only. **Judgment call accepted**: it also corrected one pre-existing README sentence claiming releases carry no checksums or signatures, which this plan made false. **BLOCKED ON A MAINTAINER ACTION BEFORE MERGE**: the signing job **fails the release when `RELEASE_SIGNING_KEY` is unset**, so the secret must exist *before* this reaches `main` or the next tag breaks. Runbook: run keygen from the worktree piping stdout straight into `gh secret set` (never to screen or history), paste the public half into `release_key.go`, then merge and tag. | **Hard prerequisite for the in-browser update the maintainer asked for.** `grep -icE "sha256|checksum|cosign|sign|attest|provenance" .github/workflows/release.yml` → **0**: releases are raw binaries attached via `action-gh-release` with nothing standing behind them, so an updater would trust GitHub account security and TLS alone — in a process holding the Garage **admin token**. **ed25519 + SHA-256, not sigstore**: `crypto/ed25519` and `crypto/sha256` are **stdlib**, so verification adds zero dependencies to a `CGO_ENABLED=0` binary and makes no network calls at verify time; the public key ships inside the binary being updated. Adds `backend/cmd/relsign` (keygen/sign/verify, stdlib only, private key read **only from the environment** so it never lands in a process table or CI log) and `backend/release_key.go` holding the **public** half. **Structural finding**: `release.yml` is a **matrix**, so no job ever sees both binaries — SHA256SUMS needs a third `needs: binaries` job, and producing it inside the matrix would race two files onto one name. The signing step **fails the release** when the secret is missing: an unsigned release is worse than a failed one, because 050 would have nothing to check while operators assume it is fine. **Executor is forbidden from generating a keypair** — it builds the tooling; the maintainer runs keygen, stores the private half as a GitHub secret and pastes the public half. `releasePublicKey` is committed **empty**, and empty must fail closed everywhere downstream. |
| [050](050-in-browser-update.md) | In-browser update — download, verify, stage | **DONE — MERGED to `main` (2026-08-18).** Merged on maintainer's instruction with the manual acceptance checks still outstanding (tamper refusal, no-key build, viewer 403, Docker hides button) — run them against the next signed release.** Sonnet executor, branch `advisor/050-in-browser-update`, commit `01c399d`, from `main` `947879d`. **Reviewer re-verified**: typecheck 0, **209 tests** / 1 pre-existing skip, build 0; Go gofmt/vet clean, all 7 packages `ok`. Scope exactly 10 in-scope files; `release_key.go`, `cmd/`, `middleware/`, `browse.go` diffs **empty**; `update.go` touched only for `CanSelfUpdate` across all four response paths. **The security ordering was verified by reading `Apply` line by line, not by trusting tests** — tests pass regardless of where a check sits. Confirmed sequence: `requireAdmin` → **empty-key refusal** → deployment gate → single-flight → body decode → *then* the first network call; and `verifyChecksumsSignature` (L70) strictly before `checksumFor` (L77), so no hash from `SHA256SUMS` is used until its signature verifies; and `ResponseSuccess` (L111) before the restart signal (L118), so the browser always gets an answer. `grep -nE "os/exec|exec.Command|syscall.Exec|os.Exit"` → matches only in **comments** explaining why they are absent. Nothing executes what it downloads. **Reviewer ran three mutations directly**: returning `nil` for an empty key fails the fail-closed guard; substring instead of exact name matching fails the prefix test (`…-amd64-evil` must not satisfy `…-amd64`); removing the empty-key refusal from `Apply` fails the refusal test — which also asserts **no outbound request was made**, so "refuses" means before touching the network. *(A first attempt at the prefix mutation failed to apply — pattern mismatch — and was redone against the real comparison; a mutation that doesn't apply proves nothing.)* **Three judgment calls, all accepted**: asset URLs built deterministically from the release tag via a `assetDownloadURL` package var (mirrors the existing `releasesURL` testability pattern) rather than adding fields to `githubRelease` — safe because integrity comes from the signature, not the URL's provenance, so a wrong URL still fails closed; `checksumFor` rejects **any** duplicate entry rather than only conflicting ones (stricter, safer reading); and `applyResponse` carries an extra `restarting` field the UI uses. **Full happy-path test deliberately absent** — exercising `Apply` end to end needs a fake `os.Executable`, which the plan forbids faking; the executor took the escape hatch and said so rather than improvising. **MANUAL CHECKS OUTSTANDING AND THEY ARE THE REAL ACCEPTANCE TEST** — nothing here has installed a real binary: (1) no-key build shows no button and refuses without a network call; (2) a real signed release stages, leaves `<exe>.bak`, and runs after a manual restart; (3) **the tamper tests** — a binary not matching `SHA256SUMS`, and an invalid `SHA256SUMS.sig` — must both refuse, leave the running binary untouched, and leave no temp file; (4) viewer gets 403; (5) Docker shows no button. | The feature as asked, built so it is defensible: admin-only button that downloads the release, **verifies the ed25519 signature over `SHA256SUMS` before trusting any hash inside it**, checks the binary's SHA-256 against the entry for its exact asset name, then atomically renames it into place keeping `<exe>.bak`. **Deliberately does not restart by default.** A process cannot reliably restart itself: if it exits and the service manager is not configured to bring it back, the admin console is gone and the only way in is SSH — precisely when the operator most needs it. Default stages the binary and reports that a restart is required; `restart: true` is opt-in behind a separate unchecked box, and triggers the **existing graceful-shutdown signal path** in `main.go` rather than `os.Exit`, so the response reaches the browser first. **Four security properties pinned by tests and mutations**: empty key → refuse (never "skip verification"); signature verified **before** the manifest is parsed; **full-name** asset matching (`…-amd64-evil` must not satisfy `…-amd64`); and **nothing is ever executed** — done criteria assert no `os/exec`, no `exec.Command`, no `os.Exit`, not even a `-version` sanity run of the downloaded file. Single-flight guard because two renames racing onto one path is how a half-written binary gets installed. **Reuses the plan-044 deployment probe** as the gate, so Docker (uid 65532 vs root-owned `/main`) never sees the button, and reuses `fetchLatestRelease`. Cross-package access to the key follows the repo's `AppVersion` injection pattern — `release_key.go` is `package main` and Go forbids importing it, the same wall plan 041 hit. |
| [051](051-relsign-pipe-safe-keygen.md) | Make `relsign keygen` safe to pipe | **DONE — MERGED & SHIPPED in v3.7.0** (`2ae81cc`). **⚠️ ADVISOR ERROR that broke two releases.** Plan 049 asked keygen to write the private key to stdout "clearly labelled" while the documented setup step pipes that stdout into `gh secret set` — mutually incompatible, so the secret received the label text and the release job failed with `invalid byte: U+0050 'P'` (the `P` of `PRIVATE KEY`). The advisor then compounded it by issuing the regeneration command *before* the fix had merged, so the second attempt regenerated with the old tool and failed identically. Establishes the rule: **stdout is for the machine, stderr is for the human** — a tool whose output is piped must put only the payload on stdout. Also confirmed by the executor that the plan's second premise was already satisfied (`decodePrivateKey`/`decodePublicKey` already trimmed whitespace), so `TrimSpace` was never the bug and the advisor overstated what was broken; the whitespace tests were added anyway and proven load-bearing by mutation. Three mutations pinned, including restoring the label. |
| [052](052-revalidate-session-against-store.md) | Revalidate every authenticated request against the user store | **DONE — MERGED to `main` `9b544f5` (2026-08-18), 1 revision.** Sonnet executor, worktree `.claude/worktrees/agent-acb54b3482a48f9c9`, branch `advisor/052-revalidate-session-against-store`, commit `b2c6965`, from `main` `947879d`. **Reviewer re-verified**: gofmt/vet/build clean, `go test -race ./...` all packages `ok`; scope exactly the 4 in-scope files; both scope diffs empty; the `admin_users_test.go` diff is exactly +1/−0 (the `bob` seed). **Reviewer ran the decisive mutation directly**: restoring the session-role read in the viewer gate fails both role-direction tests (demoted admin keeps 200 → FAIL; promoted viewer gets 403 → FAIL); executor's other two mutations (nil-user allow, ignore Disabled) each failed their named test. Cache is a bounded local map (5s TTL, negative caching, 1024-entry sweep, injectable clock — TTL tested at ±1ms, no sleeps). **⚠️ ADVISOR ERROR — the original scope locked out the fix its own change forces.** `TestAdminUsersAuthorizationEndToEnd` drives the real router through `AuthMiddleware` with a viewer session for `bob` and no `bob` store row; under Step 1.3 a phantom-username session is correctly 401, so five subtests expecting 403 failed while the plan forbade touching `backend/router/`. The executor STOPPED correctly instead of improvising. Plan amended: one additive `seedUser(bob, viewer)` line, preserving the test's original 403 intent. Lesson: a plan that changes what the middleware trusts must audit *pre-existing tests that fabricate sessions* — they are callers of the changed contract. Live check NOT performed: disable a user in the UI and watch their open session die within 5s. **Found independently by two auditors.** `middleware/auth.go:89-97` reads `authenticated` and `role` from the session and never consults the store again, so **disabling, demoting or deleting an account has no effect on an open session** for up to 24h absolute / 2h idle — a demoted admin keeps `/admin/users` and every write through the proxy that carries the cluster-wide Garage admin token. **This is documented incident response that does not work**: `docs/authentication.md:346` tells operators to disable the account to kill sessions. Decision drift — doc and code disagree; the plan makes the code match the doc. Resolves the caller per request via `GetUserByUsername`, whose **`(nil, nil)` for not-found is the single most likely way to get this wrong** and is called out as such. Uses `user.Role`, never the session copy, and explicitly forbids writing the fresh role back into the session (scs would persist it, making the untrusted value load-bearing again). 5s bounded local cache with negative caching, because the SQLite pool is capped at one connection; `utils.Cache` is forbidden here (global, unswept). |
| [053](053-delete-thumbnail-endpoint.md) | Delete the thumbnail endpoint — two unbounded memory paths on dead code | **DONE — MERGED to `main` `69941b9` (2026-08-18).** Sonnet executor, worktree `.claude/worktrees/agent-a5f5a892b2d3436c5`, branch `advisor/053-delete-thumbnail-endpoint`, commit `a3f352e`, from `main` `947879d`. **Reviewer re-verified**: gofmt/vet/build clean, `go test -race ./...` all packages `ok`; frontend typecheck 0, 203 tests / 1 pre-existing skip. Scope exactly the 6 in-scope files (+74/−153); `src/` and `backend/middleware/` diffs **empty**; `image.go`/`image_test.go` gone; `go.mod` diff is exactly the one `nfnt/resize` line; `grep thumb browse.go` → 0; the guard is `if !view && !download` with the `view`/`download` branches byte-untouched. **Reviewer mutation-tested the guard directly** — a first attempt (`if false`) failed to *build*, which proves nothing (same lesson as the 049 review); redone as an inversion (`if view \|\| download`), which compiles and fails the new metadata-path test exactly as designed (bare request mis-routes to GetObject, mock 404s any non-HEAD → status 500 ≠ 200). No pre-existing test referenced `thumb`, so Step 4's deletion clause was a no-op; the new `TestGetOneObjectNoQueryParamsTakesMetadataPath` uses the plan-055 fixture seam (`t.Setenv` admin+S3 mocks). README/docs never documented the parameter — nothing to remove. Historical `thumb` *comments* remain in 4 src files (allowed by the criterion, noted). Original finding — **best trade in the audit: deleting code removes the exposure, ~130 lines and a dependency.** `browse.go:143-160` does `io.ReadAll` with no cap, then `utils/image.go:16` does `image.Decode` with no dimension check — **two independent memory bombs**, and capping the download does not fix the second: a ~1 MB PNG declaring 30000×30000 allocates ~3.6 GB. Reachable by the **read-only viewer** (it is a GET), with object bytes chosen by anyone holding S3 write access to the bucket — not only console users. **And nothing calls it**: `grep -rn thumb src/` yields a single comment, because plan 047 replaced row thumbnails with generic icons. Removes the branch, `utils/image.go`, `image_test.go` and `nfnt/resize`. The one real hazard is the `if !view && !download && !thumbnail` guard, which decides whether the metadata path is taken — the plan isolates it and adds a test. Frontend code actually setting `thumb=1` is a STOP condition, not something to edit. |
| [054](054-login-limiter-proxy-aware.md) | Make the login limiter usable behind a reverse proxy | **DONE — MERGED to `main` `8a2ff5b` (2026-08-18), 1 revision.** Sonnet executor, worktree `.claude/worktrees/agent-a021aef5ec92232e9`, branch `advisor/054-login-limiter-proxy-aware`, commits `7956eb6`+`bbf10f9`, from `main` `947879d`. **Reviewer re-verified**: gofmt/vet/build clean, `go test -race -count=1 ./...` all packages `ok`; scope exactly the 5 in-scope files; `backend/middleware/`, `session.go`, `src/` diffs **empty**. Three isolated buckets (`loginAttempts`/`passwordChangeAttempts`/`setupAttempts`), login keyed `(clientAddr, username)`, `ChangePassword` keyed on username, opt-in `TRUSTED_PROXY_HEADER` (default-off, last-hop), once-per-window sweep under the existing mutex. **⚠️ REVISION — the reviewer's diff read found two gaps the plan's own tests could not see, both introduced or exposed by the keying change.** (1) Username keying forces `json.Decode` *before* the rate-limit check, silently removing the old implicit bound where a 429'd client never triggered a body parse — an uncapped attacker-chosen read on an unauthenticated endpoint; fixed with `http.MaxBytesReader(…, 1<<20)` + an oversized-body test (2 MiB → non-200, no `Set-Cookie`). (2) `r.Header.Get` returns only the **first header line**, but HAProxy-style proxies *append a separate* `X-Forwarded-For` line rather than merging (nginx-style) — so a client-sent first line won and the client picked its own bucket, defeating the exact last-hop rule the plan mandates; fixed with `Header.Values` → last line → last entry + a two-line spoof test. **Reviewer ran both decisive mutations directly**: reverting `Values`→`Get` fails the last-line test; dropping the username from the login key fails the same-address isolation test. Executor's earlier mutations (first-vs-last entry, sweep removal) also confirmed. Executor judgment accepted: `Setup.Create` moved to `clientAddr` (address-only key — the account doesn't exist yet). Lesson for future plans: when a step *reorders* operations around a security check, the plan must ask what the old ordering implicitly bounded. Live check NOT performed: behind a real proxy with the header set, two users at one address get separate budgets. Original finding — **Found independently by two auditors, and it breaks in the deployment this repo documents — the maintainer's own.** `auth.go:96-102` keys on `RemoteAddr`, so behind a reverse proxy every user shares one 10/min budget: ten failed logins from one person lock out **login and password change for everyone**, and a loop keeps the console permanently unauthenticatable — a limiter that converts one attacker into denial of administration. Two compounding defects: the budget is **shared** with `ChangePassword` and `Setup.Create`, and `allow` rewrites `l.attempts[key]` but never deletes it, so the map grows for the process lifetime. Fix keeps the existing (correct) refusal to blind-trust `X-Forwarded-For` and makes it **opt-in** via `TRUSTED_PROXY_HEADER`, **default-off so upgrades change nothing**; uses the **last** XFF hop (the first is client-writable); keys login on `(addr, username)`; separates the three buckets; adds a sweep. Trusting the header by default, or reading the first hop, are both STOP conditions. |
| [055](055-s3-handler-test-fixture.md) | Handler-level tests for the S3 data plane, using the fixture that already exists | **DONE — MERGED to `main` (2026-08-18), 1 revision.** Auto-merged cleanly with 053's `browse_test.go` addition; full `-race` suite green post-merge. Unblocks 056.** Sonnet executor, branch `advisor/055-s3-handler-test-fixture`, commits `4e857bf`+`1ab247c`, from `main` `947879d`. **Reviewer re-verified independently**: scope exactly the two `_test.go` files with **zero production files touched**; no seam introduced; every `newS3Fixture` bucket name unique; `TestDownloadArchive` kept every original assertion (no `t.Errorf`/`t.Fatalf` removed); 68 new assertions; gofmt/vet/build clean and all packages `ok`. **Coverage reproduced on the reviewer's own run**: `GetObjects` 0→79.2%, `GetOneObject` 0→63.6%, `PutObject` 0→66.7%, `DeleteObject` 0→76.9%, `BulkDeleteObjects` 0→61.3%, `ListMultipartUploads` 0→71.4%, `AbortMultipartUpload` 0→66.7%, `ShareObject` 0→81.0%, `buckets.go:GetAll` 0→80.6%; package **57.4% → 77.9%**. **⚠️ REVISION — the reviewer's mutation caught a flaky guard.** Widening `sem := make(chan struct{}, maxBucketInfoConcurrency)` to 64 failed the new concurrency test only **1 run in 5**: the release gate opened as soon as one goroutine observed the 8th arrival, so with a wider cap the first eight completed before the rest arrived and `peak` never climbed. Particularly worth catching because this test exists *specifically* to replace one that asserted nothing about the real handler — a flaky replacement is a quieter version of the same defect. Revised so every fake handler blocks unconditionally and the **test** releases them: wait for the cap to be reached (5s timeout, `t.Fatal`), settle 50ms via `time.NewTimer` so bound-violating arrivals can land, snapshot and assert `peak`, then release. That inverts the timing dependence — a longer wait now makes the test *stricter*, where before a faster machine made it weaker. **Re-verified by the reviewer: 5/5 FAIL under the mutation (was 1/5), 10/10 PASS with the real cap.** Executor also verified the fixture's wire shape against aws-sdk-go-v2's own `deserializers.go` rather than assuming it — a wrong shape fails as an opaque SDK error, not a clear assertion. Noted but correctly **did not fix** the two `browse.go` defects it saw while writing these tests; plan 056 owns them. **Two reviewer process notes**: a first mutation attempt ran `sed` in the wrong directory so nothing changed and the resulting pass proved nothing; and a single passing mutation run is not evidence — the flakiness only surfaced across five. | Eight `browse.go` handlers measure **0.0%** coverage — every existing test in that file covers a *pure helper*. **⚠️ The advisor corrected an auditor here.** The correctness agent reported "no seam exists to inject a fake S3; `getS3Client` constructs its client inline". **That is false**: `browse_test.go:921-975` already injects one, because `getS3Client` resolves `utils.Garage.GetS3Endpoint()` **at call time**, so `t.Setenv("S3_ENDPOINT_URL", …)` *is* the seam. The plan therefore changes **zero production code**, where the reported version would have refactored a security-sensitive file. It also forbids an interface seam for a concrete reason: `browse.go:875` calls `s3.NewPresignClient(client)`, which needs the concrete `*s3.Client`. Extracts the fixture into `newS3Fixture`, covers the read/write/multipart/share handlers, and replaces `buckets_test.go`'s re-implementation test (never enters `GetAll`, measures 0.0%, holds the backend suite's only `time.Sleep`). Per-test unique bucket names are mandatory — the credential cache is bucket-keyed with a ~1h TTL. Touching any non-test file is a STOP condition. |
| [056](056-object-stream-defects.md) | Fix the two object-streaming defects — silent corruption and a nil-pointer panic | **TODO — EXECUTABLE NOW. P1 · S · LOW. Gate on 055 satisfied** (055 merged `6f0cea8`). Refreshed at reconcile 2026-08-26 (`6a36683`): both defects re-verified present at `browse.go:145` and `:184-187`; line refs updated, code unchanged since stamping. Two defects forty lines apart, both invisible when they occur. **(1)** `browse.go:203-208` calls `utils.ResponseError` after `io.Copy` has already written body bytes; its `WriteHeader` is a no-op but it still **appends `err.Error()` to the body**, so the client saves N valid object bytes plus an English error string under the object's real content type with a now-wrong `Content-Length` — silent data corruption no client check can catch, since the frontend only inspects `res.ok`, which is `true`. **(2)** `browse.go:164` dereferences `object.LastModified` unguarded while `ContentLength` and `ETag` **are** guarded thirty lines below — the asymmetry proves oversight, and every image/video/PDF preview crosses that line. `DownloadArchive` in the same file already handles the identical hazard correctly, which is the pattern the fix follows: **once a body has started, the only honest failure is truncation** — log and return. Buffering the object so an error can still be reported is a STOP condition; it would reintroduce the unbounded-memory pattern this codebase has been removing. |
| [057](057-correct-stale-and-wrong-docs.md) | Correct the docs that are actively wrong | **TODO — EXECUTABLE NOW. P2 · S · LOW · no deps. Refreshed at reconcile 2026-08-26 (`6a36683`)**: defects 1–2 re-verified (doc still says two viewer writes vs three in `auth.go:52-54`; `CLAUDE.md` still says 3.1.0 vs `package.json` 3.8.0). Defect 3 **rewritten**: CI moved to Jenkins (`fabb2db`), `ci.yml` deleted, pin is now `1.25.13` in three in-repo sites; Step 3 now also fixes `CLAUDE.md`'s "CI is ci.yml" claim and removes the dead README CI badge. Row text below is the original summary. Three defects of the *actively wrong beats missing* kind. **(1)** `docs/authentication.md:154` states as an absolute that viewers may write to exactly two routes; `middleware/auth.go` permits **three** (`/browse/download-token`, deliberately, rationale in the code) — a security doc understating the read-only role's write surface. **(2)** `CLAUDE.md` announces "Current release (3.1.0)" and plans 001–033 when the tag is **v3.7.0** with 51 plans; it is the file every dispatched agent reads first, so the staleness propagates into work. **(3)** it names **four** Go pin sites; there are **five** — `release.yml` pins twice, the doc names one, and the unnamed one is the release-**signing** job. All five agree today, so there is no drift; the risk is that the doc is the renewal checklist. Fix makes the release block **derived rather than stated** (pointing at `git describe` and `plans/README.md`) precisely because the previous text was accurate when written and wrong six releases later. Also documents `API_ADMIN_KEY` — read in `utils/garage.go`, absent from both README and `.env.example` while the README claims a complete reference — **by name and purpose only, never a value**. Writing a version number into `CLAUDE.md` is a STOP condition. |
| [058](058-allow-key-dialog-silent-submit.md) | Make "Allow Key" fail loudly instead of reporting success for an empty submit | **DONE — MERGED to `main` `b3dc748` (2026-08-26).** Sonnet executor, worktree `.claude/worktrees/agent-a12f241c7b56acb6a`, branch `advisor/058-allow-key-dialog-silent-submit`, commit `80ed022`, from `main` `6a36683`. **Reviewer re-verified independently**: typecheck 0, eslint clean on the touched dir, `pnpm run test` 19 files / **214 passed** / 1 pre-existing skip (+5), build 0. Scope exactly the 3 in-scope files (+215/−10); `hooks.ts` and `backend/` diffs **empty**; exactly one `eslint-disable` (the reseed effect, with the explanatory comment). **Reviewer ran two mutations directly**: reverting `useWatch` to `form.watch` fails the auto-select test; deleting the empty-submit guard fails the nothing-ticked test. **Documented deviation, accepted on merit**: the plan's Step 2 snippet used `form.watch("keys")`, which in react-hook-form v7 does not re-render on Controller-driven nested changes, so the auto-select effect never re-fired; executor switched to `useWatch({ control, name: "keys" })` and proved it with the test — **advisor error in the plan snippet**, executor was right. Tests assert exact mutate payloads and toast strings, and the survive-rerender case rerenders with a fresh `[]`. **Not verified in a browser** — maintainer should confirm a `POST /api/v2/AllowBucketKey` now fires from the dialog; if Garage answers 4xx, that body is the real cause. _Was:_ TODO — P1 · S · LOW · no deps (stamped `6a36683`, 2026-08-26). Maintainer report: "giving a bucket permissions will not succeed". Request shape verified correct against the Garage v2 spec; backend proxy untouched. Two dialog defects: only rows with the left-most **Key** box ticked are sent, so ticking permissions alone yields an empty payload, `Promise.all([])` resolves, and a green "Key allowed!" toast fires with nothing done; and `currentKeys` is a fresh array every parent render, so the seeding effect wipes selections on any bucket refetch. Fix: reject empty / permission-less submits with a toast, auto-select a row when a permission is ticked, memoise `currentKeys` and reseed only when the candidate id set changes. Five jsdom tests. |
| [059](059-cross-compile-multiarch-image.md) | Build the multi-arch image without QEMU — cross-compile both stages on the build host | **DONE — MERGED to `main` `0dfe0ac` (2026-08-27).** Sonnet executor, worktree `.claude/worktrees/agent-aa736dc5b13e0ea2a`, branch `advisor/059-cross-compile-multiarch-image`, commit `3b728a2`, from `main` `8e062cb`. Executor was killed by a session limit mid-timing and resumed; the edits survived intact. **Reviewer re-verified independently** with a fresh `docker-container` builder: multi-arch build exit 0, `file` on the arm64 output → `ELF 64-bit LSB executable, ARM aarch64`, amd64 → `x86-64`, and **0** `[linux/arm64 frontend|backend]` steps in the log; `FROM --platform=$BUILDPLATFORM` ×2, `GOARCH=${TARGETARCH}` ×1, `ARG TARGETARCH` inside the backend stage (line 48), runtime `FROM` untouched, `ci.yml` gone from the Dockerfile comment. Scope exactly `Dockerfile` + `Jenkinsfile` (+22/−10); no `out-*`/`build.log` left; builder removed. **Timings (executor)**: cold before **2m59s** (arm64 `go build` under QEMU alone 68.7 s); after, the arm64 go-build step **6.5 s** with a pruned cache, full multi-arch **15 s** cache-warm. A cold end-to-end "after" was not re-measured after the resume. **Executor flagged, correctly left alone**: the Jenkinsfile header comment still says `github-pat` while the pipeline uses `ghcr-pat` — trivial follow-up, not in this plan. _Was:_ TODO — P2 · S · LOW · no deps (stamped `8e062cb`, 2026-08-26). From the Jenkins log of build #4 (2026-08-26): the `Dockerfile` runs `frontend` and `backend` stages *inside* the target arch, so the arm64 half runs under QEMU — arm64 Vite build **121.7 s vs 10.8 s** native, and the arm64 `go build` was still running when `abortPrevious` killed the build. Fix: `FROM --platform=$BUILDPLATFORM` on both build stages + `GOARCH=${TARGETARCH}` for `go build`; runtime stage stays per-platform. Gates: `file` on the arm64 artifact says `aarch64`, and the multi-arch build log has zero `[linux/arm64 frontend|backend]` steps. Also corrects the Dockerfile's stale `ci.yml` comment. The Jenkins double-trigger (indexing + poll both scheduling a build) is a Jenkins-side setting, documented in the maintenance notes, not fixed by the repo. |
| [060](060-file-suffix-secrets.md) | Read secrets from files — `_FILE` suffix for `API_ADMIN_KEY` and the legacy credentials | **TODO — v4 · P2 · S · LOW · no deps** (stamped `8e062cb`, 2026-08-26). Maintainer picked this from a comparison with `Noooste/garage-ui`. Compose `secrets:` / K8s Secret volumes deliver values as files; today the admin token can only be an inline env var. Adds `utils.GetSecretEnv(key)`: `KEY_FILE` wins over `KEY`, read once, trailing CR/LF stripped, unreadable file is `log.Fatalf` (path only, never the value). Wired into `GetAdminKey` and the legacy import; five unit tests; README/.env.example/auth-doc rows. Secrets only — non-secret vars deliberately excluded. |
| [061](061-object-search.md) | Search a bucket for objects by name — recursive substring search with a hard scan cap | **TODO — v4 · P2 · M · LOW · no deps** (stamped `8e062cb`, 2026-08-26). Same source. S3 has no search; this is a bounded server-side walk of `ListObjectsV2` *without* a delimiter under the current prefix: case-insensitive substring on the relative key, stops at **200 matches or 20 pages (20 000 keys)** and says which (`truncated`, `reason`). Own route `GET /search/{bucket}` so it cannot shadow an object named `search` under `/browse/{bucket}/{key...}`; GET ⇒ viewer-allowed with no middleware change. Six handler subtests on the existing `newS3Fixture` (the scan-cap test needs the page size/page cap as test-overridable vars). Frontend: debounced search box in the navigator's `actions` slot, ≥ 2 chars, flat `SearchResults` table replacing the folder list, row click navigates to the parent folder, truncation banners. `object-list.tsx` untouched. |
| [062](062-port-release-to-jenkins.md) | Port the signed-binary release from GitHub Actions to Jenkins | **DONE — MERGED to `main` `9ac5018` (2026-08-27); Jenkins-side steps pending.** Sonnet executor, worktree `.claude/worktrees/agent-acd7b85c7b5a65e0d`, branch `advisor/062-port-release-to-jenkins`, commit `cd4eec7`, from `main` `012066a`. **Reviewer re-verified independently**: declarative validator → `Jenkinsfile successfully validated.`; 4 `when { tag pattern: 'v*', comparator: 'GLOB' }` stages; `relsign-key` and `github-release-token` each exactly once; `RELEASE_SIGNING_KEY` only in `withCredentials`/`-key-env`, never echoed; `release.yml` deleted and unreferenced; `backend/` diff empty; scope exactly 4 files (+117/−122). Executor's local dry run produced aarch64 + x86-64 binaries and a 2-line `SHA256SUMS`. **Accepted deviation**: the stage comment said "…retired GitHub Actions release.yml…", which the plan's own `grep release.yml` gate forbade — executor reworded to "release workflow" (advisor error in the plan). Executor noted the worktree host lacked `go`; it used mise Go 1.25.14 for local verification only — no pin touched. **Before the first tag build the maintainer must**: enable *Discover tags* on the `d7eeem` org folder (API write was blocked by the auto-mode classifier; do it in the UI or via `!`), and create secret-text credentials `relsign-key` and `github-release-token`. Then re-push `v3.9.0`. _Was:_ TODO — P1 release blocker · M · MED · no repo deps (stamped `012066a`, 2026-08-27). `v3.9.0` was tagged and its `release.yml` run queued forever: `runs-on: self-hosted` with **0** runners (retired at the Jenkins move). Four `when { tag 'v*' }` stages in the `Jenkinsfile`: tag↔package.json check, cross-compiled amd64/arm64 binaries + `SHA256SUMS` + `relsign` signature (credential `relsign-key`), GitHub release via REST with `curl`+`node` (credential `github-release-token`, contents:write — `github-pat` is read-only), and versioned image tags `:X.Y.Z`/`:X.Y`. `release.yml` deleted. Asset names/`relsign` untouched (contract with `relsign verify` and the in-app updater). **Jenkins prerequisites for the maintainer**: the org folder has no `TagDiscoveryTrait` (verified via config.xml) — enable *Discover tags*; add the two credentials. Gates: declarative validator endpoint + local dry-run of the build half. |

> ### 🔗 Unmerged advisor stack (2026-08-06) — merge as ONE fast-forward
>
> Plans 034, 035, 031, 033, 036 were executed **in sequence, each branching from the previous**, not from `main`. That was deliberate: 031 shares `backend/router/browse.go` with 034, and 036 shares the `README.md` env-var table with 034, so five independent branches would have conflicted. The result is one linear stack on top of `796039f`:
>
> ```
> main (796039f)
>  └─ advisor/034-upload-limits-and-real-errors          7274b07
>     └─ advisor/035-upload-progress-panel               52122bf
>        └─ advisor/031-download-selected                ece5578
>           └─ advisor/033-header-account-settings       26fd772
>              └─ advisor/036-share-dialog-truth-…       6bdcb25   ← merge this one
> ```
>
> **Merging `advisor/036-share-dialog-truth-and-public-website-url` brings all five.** Each was reviewed and approved independently (gates re-run by the reviewer, scope checked against the diff, new tests mutation-tested). Nothing was pushed or merged — that is the maintainer's call.
>
> **Not yet verified against a live Garage instance.** No worktree had one. The highest-value manual checks before merging: an over-limit upload returns a real **413** (034); the upload panel shows that message rather than "NetworkError" (035); *Download selected* produces a ZIP and its URL 404s on replay, and a **viewer** can download but still cannot delete (031); the header chip reaches `/settings` (033); the Share dialog explains itself with `S3_PUBLIC_ENDPOINT_URL` unset, and `S3_WEB_PUBLIC_URL` rewrites the website URL (036).

### Uploads & sharing (`/improve plan`, requested 2026-08-06, stamped `f9cd2c6`…`796039f`)

Three plans from one maintainer report against the live deployment: *"clicking
the share button gives me `http://assets.web.…:3902/hp/dockhand-white.png`, on
upload I get network error"*, plus a request for an upload indicator.
**Execute 034 → 035; 036 is independent.**

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|---|---|---|---|---|---|---|
| [034](034-upload-limits-and-real-errors.md) | Make a failed upload return a real HTTP error instead of a dropped connection | P1 | S | LOW | — | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/034` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Executed by a Sonnet executor in worktree `.claude/worktrees/agent-a7d84563eede35a1d`, branch `advisor/034-upload-limits-and-real-errors`, commit `7274b07`. **Independently re-verified by the reviewer** on the repo's *pinned* toolchain (`golang:1.25.12`, not the executor's floating `golang:1.25`): `gofmt -l .` silent, `go vet` clean, build exit 0, `go test -race ./...` all 6 packages `ok`. Scope clean — `git diff --stat main..HEAD` is exactly the 3 in-scope files (+98/−1). `TestMaxUploadBytes` **mutation-tested**, not just run: `mb <= 0`→`mb < 0` fails the zero case, `<< 20`→`<< 10` fails the unit case — the test catches real regressions. All 4 `drainRequestBody(r)` calls confirmed to sit before their `ResponseError` and be followed by `return`; the `!isDirectory` guards were not restructured, so folder creation (an empty `FormData`, which parses as a valid zero-part multipart body) still short-circuits both error branches. Live Garage check **not** performed — no instance in the worktree; the executor said so plainly rather than claiming it. (stamped `796039f`) — "network error" is not this codebase's string: it is Firefox's `TypeError: NetworkError when attempting to fetch resource`, toasted verbatim by `handleError`. The browser emits it when the socket is torn down mid-body, which is what happens when `PutObject` answers with an error and returns while the browser is still uploading — Go stops draining and closes, so the real status never lands. Adds `MAX_UPLOAD_SIZE_MB` (default 512, checked against `r.ContentLength` **before** `ParseMultipartForm` buffers anything) → a real **413**; `MaxBytesReader` as the belt; `drainRequestBody` before every other error return; and `ParseMultipartForm(4 MiB)` so payloads spill to disk instead of holding 32 MiB of RAM per concurrent upload. Backend-only, 3 files. |
| [035](035-upload-progress-panel.md) | Upload progress panel with per-file cancel and real error messages | P1 | M | MED | 034 (soft) | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/035` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-aeec46122b398d208`, branch `advisor/035-upload-progress-panel`, commit `52122bf`, **stacked on `advisor/034-…`** (not `main`). **Reviewer re-verified**: typecheck 0, `pnpm run test` 74 passed/1 pre-existing skip across 8 files, `pnpm run build` 0, eslint on the 3 new files **0 problems**. Scope exactly the 7 in-scope files; `hooks.ts` byte-identical to base (0-line diff) so folder creation is provably untouched. **Mutation-tested**: dropping the `canceled` guard in `onSuccess` fails the resurrection test; `MAX_CONCURRENT_UPLOADS` 3→20 fails 3 concurrency tests. Two executor deviations, both **approved on merit**: (a) reworded one doc comment to avoid the literal `Content-Type` because the plan's own done-criterion grep forbade a string its own Step 3 prose told the executor to write — the code provably never calls `setRequestHeader("Content-Type", …)` and a test asserts it; (b) added a module-level `pendingFiles: Map<string, File>` because `UploadItem` (as the plan specified it) carries no `File`, so the pump had nowhere to find the bytes — a genuine gap in the plan, fixed minimally and in scope. Live browser check **not** performed. (stamped `796039f`) — the maintainer's actual request. Structural root cause: **`fetch` cannot report upload progress**, so `usePutObject` is replaced *for file uploads only* by an `XMLHttpRequest` transport (`upload.onprogress`). `usePutObject` itself stays untouched — `CreateFolderAction` still uses it, and folders must not appear in a panel titled "Uploading". Adds a zustand queue (`MAX_CONCURRENT_UPLOADS = 3`, replacing today's fire-all-20-at-once loop), an in-flow panel under the navigator (deliberately not floating — no new `Z_LAYERS` entry), and per-file cancel. `xhr.status`/`responseText` is what finally surfaces 034's 413 text. Two exported `api.ts` primitives (`apiUrl`, `csrfHeader`) keep BASE_PATH and CSRF from drifting between the two transports. |
| [036](036-share-dialog-truth-and-public-website-url.md) | Tell the user why private share links are unavailable, and let the website URL point at a TLS reverse proxy | P2 | M | LOW | — | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/036` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-aadfefe8f6f090657`, branch `advisor/036-share-dialog-truth-and-public-website-url`, commit `6bdcb25`, **tip of the stack** (on `advisor/033-…`). **Reviewer re-verified**: Go gofmt/vet silent, `go test -race ./...` all packages `ok` on pinned `golang:1.25.12`; frontend typecheck 0, 9 files / 88 passed / 1 pre-existing skip (`website.test.ts` 15→**25**), build 0. Scope exactly the 9 in-scope files; `overview-website-access.tsx`, `bucket-card.tsx` and `backend/router/browse.go` all have **empty** diffs, confirming the callers improve for free and the presigner was never touched. README now carries all three env rows without disturbing the sibling's. **Mutation-tested both load-bearing properties**: moving the override check back below the `root_domain` guard fails 5 tests (incl. `uses public_url even when root_domain is entirely absent`); dropping `public_url` from `isWebsiteHostingConfigured` fails `is true when only public_url is set`. Live browser check **not** performed. (stamped `796039f`) — the share report was **not a bug in share links**; it was the fallback. `share-dialog.tsx:51` hides the entire private-link block behind `config?.sharing` (true only when `S3_PUBLIC_ENDPOINT_URL` is set) with **no message**, leaving only `getBucketWebsiteObjectUrl`, which hard-codes `http://` + the raw `[s3_web] bind_addr` port — mixed content from an HTTPS console. Adds `S3_WEB_PUBLIC_URL` (one variable; `{bucket}` token ⇒ vhost style, absent ⇒ path style) carried through `GET /config` as `s3_web.public_url`, and makes the dialog state-explicit in all four states. Also corrects a **false** string shipped since plan 015: "Sharing is only available for buckets with enabled website access" — presigned links work without website access. |

> ### ⚠️ The recurring advisor error: grep-based done-criteria. **Five occurrences now (029, 030, 034, 035, 036).**
>
> Every one is the same shape: a plan asserts `grep <identifier> <file>` returns an exact count, but the plan's **own** prose, comments, or quoted code contain that identifier too — so the criterion is unsatisfiable without mangling code the plan itself specified. In all five the executor did the right thing: implement the quoted code faithfully, flag the contradiction, and *not* treat it as a STOP condition.
>
> | Plan | Criterion | Reality |
> |---|---|---|
> | 029 | `grep "reset-password" backend/router/ backend/middleware/` empty | also matched the **pre-existing** authenticated route from plan 025 |
> | 030 | (self-contradictory step, not a grep) | required a boolean "already reloaded" flag *and* clearing it — guarantees the reload loop the same plan forbade |
> | 034 | `grep "MaxBytesReader"` = 1 | 3 — two are comments the plan told the executor to write |
> | 035 | `grep "Content-Type" upload-queue.ts` = 0 | Step 3's own doc-comment text used the phrase |
> | 036 | `grep -r "S3_WEB_PUBLIC_URL" backend/` = 1 | 2 — Step 2's quoted `config.go` comment names the variable |
>
> **Rules for future plans, in order of preference:** (1) assert on the **diff** (`git diff <base>..HEAD -- <path>` empty / lists only X), never a repo-wide grep — a pre-existing match is not a violation; (2) if you must grep, target a syntactically distinctive *call* form (`http.MaxBytesReader(`, `setRequestHeader("Content-Type"`) rather than a bare identifier; (3) before shipping a plan, grep the plan file itself for every string its own criteria count.

### Website hosting

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|---|---|---|---|---|---|---|
| [037](037-website-hosting-reliability.md) | Report whether bucket settings saved, and warn when a website's index document is missing | P2 | M | LOW | — | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/037` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a17506b96e7db6f1a`, branch `advisor/037-website-hosting-reliability`, commit `eb2cac7`, parent `fa26384`. **Reviewer re-verified**: Go gofmt clean, vet 0, `go test -race ./...` all packages `ok` on pinned `golang:1.25.12`; frontend typecheck 0, 10 files / 94 passed / 1 pre-existing skip, build 0. Scope exactly the 7 in-scope files; `browse.go` diff is two additive hunks only (the HEAD-branch guard + the helper) with nothing inside `PutObject`/`BulkDeleteObjects`/`DownloadArchive`/`CreateDownloadToken`; `website.ts`, `types/garage.ts` and `browse/` diffs all **empty**. Confirmed `...options` is spread **after** `onError`, so a caller can still override. Confirmed the presence probe reads persisted `data.websiteConfig`, **not** form state — the `useWatch` in that file is the pre-existing `isEnabled`. **Mutation-tested both**: `404`→`400` in `classifyObjectProbe` fails the "missing" case; dropping `"NotFound"` from `isNotFoundErr` fails `HeadObject_miss_(NotFound)`. Executor independently verified against the real aws-sdk-go-v2 v1.106.4 source that `types.NotFound.ErrorCode()` is `"NotFound"` and `types.NoSuchKey.ErrorCode()` is `"NoSuchKey"` — the plan's central SDK claim, confirmed rather than assumed. One sanctioned deviation: extracted the status node into `overview/save-status.tsx` (the plan explicitly permitted this). Live Garage check **not** performed. (stamped `796039f`, verified byte-identical at stack tip `6bdcb25` — **base-agnostic**, executes on `main` or on the stack). Written from `/improve plan Richer static-website hosting management surface`; maintainer scoped it (2026-08-06) to **reliability + document verification**, deferring a Website tab, CNAME guidance, and a live reachability probe. Two measured defects underpin it. **(1) Bucket settings fail silently**: `overview-website-access.tsx:32` and `overview-quota.tsx:21` both call `useUpdateBucket(...)` with no `onError`, and `manage/hooks.ts:42` supplies no default — a rejected `POST /v2/UpdateBucket` produces no toast, no revert, and the form keeps showing a value the server never accepted. **(2) The HEAD path reports a missing object as 500**: `browse.go:100-107` passes every `HeadObject` error to `utils.ResponseError`, while the GET branch at `browse.go:117-126` correctly maps `NoSuchKey`→404 — so "no `index.html`" is indistinguishable from "Garage is down". Note the SDK detail the plan pins: HEAD has no response body, so aws-sdk-go-v2 synthesizes **`NotFound`**, not `NoSuchKey`; matching only the latter silently does nothing. The `"unknown"` presence state is load-bearing — the probe rides the `/browse` path, which needs a read+write bucket key, so buckets without one can never be probed and must not be accused of missing files. |
| [038](038-public-asset-urls.md) | Permanent public asset URLs — make the website endpoint a first-class sharing path | P2 | M | LOW | **036** (hard), 037 (soft) | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/038` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge (1 revision).** Sonnet executor, worktree `.claude/worktrees/agent-aa46294aebc5fc631`, branch `advisor/038-public-asset-urls`, tip `7711b23`, **stacked on `advisor/037-…`** (both edit `overview-website-access.tsx`). **Reviewer re-verified**: Go gofmt clean, vet 0, race tests all `ok`; typecheck 0, **123** tests (website 25→39, upload-queue 12→27), build 0. 037's `hooks.ts` and `save-status.tsx` have a **0-line diff**. **Revision round (1 of 2 used):** the first submission passed all 38 tests even when `getPublicAccess` was mutated to check the URL *before* `websiteAccess` — a real behaviour change (for `websiteAccess: false` + no public URL, `private` becomes `public-no-url`, i.e. a private bucket labelled "public read enabled" — precisely the misleading-label failure §6 exists to prevent). Every `private` case supplied a working URL, so the ordering was never exercised. Executor added one ordering test, source byte-identical; reviewer re-ran the mutation and it now fails exactly that test. **Sanctioned deviation:** `mime/lite` returns `null` for `.ico` (reviewer reproduced independently — svg/webp/avif all resolve), which is the plan's own STOP condition whose prescribed remedy is a Go-stdlib fallback; `resolveUploadContentType` in `browse.go` is one helper plus one call site and preserves any non-generic client-supplied type. **Accepted judgment call:** Copy-URL renders only for the mounted bucket, because a foreign bucket's `websiteAccess` is unreachable without touching out-of-scope `hooks.ts` — the URL itself is still built from `item.bucket`, per the plan's CRITICAL note. **PHASE 7 DONE (2026-08-10)** — Sonnet executor, worktree `.claude/worktrees/agent-a5873b718308cf431`, branch `advisor/038-phase7-drop-path-style`, commit `03ee680`, based on `advisor/038-public-asset-urls` @ `7711b23`. **Merge THIS branch, not 7711b23.** Reviewer re-verified: typecheck 0, 124 tests (website 39→40), build 0; scope exactly the 5 in-scope files; `getPublicAccess`'s body unchanged. **Mutation-tested**: restoring `return \`${template}/${bucketName}\`` fails **3** tests across all three layers (`getBucketWebsiteBaseUrl`, `getBucketWebsiteObjectUrl`, `getPublicAccess`). The executor changed **more tests than the plan named** — reviewer checked each: two were direct inversions of path-style assertions; three were retargeted to `{bucket}` templates while preserving their original subject (trailing-slash stripping, public_url-over-root_domain precedence, empty-bucket-name handling); one is new. None was weakened, and *"returns null with no bucket name"* got **stronger** — with a token-less template it would have returned null for two reasons at once and could have passed for the wrong one. **Advisor error #7 of the grep-criteria family, again caught by the executor**: a done-criterion required `git diff -- backend/` to be empty while the plan's own Scope section named `backend/.env.example` as in-scope. The executor treated the explicit file list as authoritative and proved the rest of `backend/` untouched with a `':!backend/.env.example'` pathspec — correct. Original text of this note: ⚠️ PHASE 7 ADDED 2026-08-10 — NOT YET EXECUTED. Live deployment disproved the **path-style** addressing mode that this plan inherited from 036: Garage's website endpoint is **virtual-host only**, resolving the bucket from the `Host` header against `[s3_web] root_domain`. Measured on a real instance, same object, differing only in `Host` — `assets.web.…` → **200 `image/png`**; `web.…` + `/assets/…` → **404**; `web.…` + `/…` → **404**. The invented fallback is live in **four** places on this branch (`src/lib/website.ts`'s `applyPublicUrlTemplate`, `README.md:169`, `.env.example:21`, `backend/.env.example:16`) — Phase 6 *propagated* the error into the last two. Phase 7 makes a token-less template return `null` (so `getPublicAccess` reports `public-no-url` and shows guidance instead of a dead link) and corrects all three documents. It also records two deployment facts that cost a real operator hours: the public hostname must route to Garage's **web** port preserving `Host`, and **TLS wildcards do not nest** (`*.example.tld` does not cover `assets.web.example.tld`; with private-IP public DNS, only DNS-01 can issue the nested wildcard). Live Garage/browser check of Phases 1–6 **not** performed. (stamped `6bdcb25`). From a detailed `/improve plan` brief (2026-08-10) for using Garage as a local static-asset host (dashboard icons, gethomepage). **Investigation overturned three of the brief's premises, and the plan leads with them:** (1) **Garage has exactly one anonymous-read mechanism and it is website access** — `backend/schema/bucket.go` has `WebsiteAccess`/`WebsiteConfig` and *no* ACL or policy field; Garage does not implement S3 bucket policies, and `Keys` are authenticated grants. So "Public Read" **is** `websiteAccess`; building a second toggle would be the parallel permission system the brief itself forbids. (2) **The requested `PUBLIC_OBJECT_BASE_URL` already exists** as `S3_WEB_PUBLIC_URL` from plan 036, already modelling both vhost (`{bucket}`) and path style, already separate from the admin and S3-API endpoints — so the plan adds **no new env var**. (3) **The brief's hard requirement is already broken**: `src/lib/website.ts:78-86` interpolates the object key raw (`${base}/${key}`), so spaces, `#`, `%` and Arabic filenames all produce broken URLs — the repo's own `encodeObjectPath` was simply never applied there. Independently corroborated by the 2026-08-10 audit as CORRECTNESS-12. Six shippable phases; the "Test Public Endpoint" button is **deferred with a reason** (a cross-origin browser fetch returns an opaque response whose status is unreadable, and a server-side HEAD is the SSRF surface already deferred in 037 — `[Open]` covers it at zero risk). Presigning is untouched: keeping it working is a regression test, not a change. |
| [039](039-public-access-opt-in.md) | Make enabling public access a deliberate act, and label private objects as private | P2 | M | LOW | **037 + 038 incl. Phase 7** (hard, **now merged**) | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/039` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a518e190df8c61563`, branch `advisor/039-public-access-opt-in`, commit `e024dc8`, from `main` `9c6ae7f`. **Reviewer re-verified**: typecheck 0, 11 files / **129 tests** / 1 pre-existing skip, build 0; scope exactly the 6 in-scope files; `src/lib/website.ts`, `backend/`, and both `hooks.ts` diffs all **empty**. Implementation is sound: the `form.watch` handler gates on `name === "websiteAccess"` so document edits keep auto-saving, and the toggle's `onChange` opens the modal on enable **without touching form state**, so Cancel leaves it visually OFF. **Mutation-tested both directions.** Making enable bypass the modal fails **3** tests — the confirmation guard is genuinely pinned. Removing the `form.watch` guard fails **0** — a recorded coverage gap, deliberately **not** sent back for revision: the security boundary is the toggle's `onChange` (pinned), and without the watch guard the worst case is a duplicate identical `UpdateBucket` write after the 500 ms debounce, which is wasteful rather than unsafe. Contrast 038, where the comparable gap produced a *misleading security state* and did warrant a revision round. Sanctioned deviation: swapped `ToggleField`→`Toggle` because the control now needs a custom `onChange`; `InputField` document fields untouched. Executor also found that react-daisyui's `Modal` keeps its body mounted when closed (only the `<dialog open>` attribute toggles) and switched to asserting `document.querySelector("dialog[open]")` — a **stronger** assertion than text presence, which would have passed spuriously. Live Garage check **not** performed. (stamped `03ee680`). From an amendment brief asking that public access be opt-in. **Investigation found 11 of its 16 acceptance criteria already pass** and the plan leads with the evidence rather than rebuilding them: `useCreateBucket` posts `{globalAlias}` only, so buckets are private by default; state is read from `GetBucketInfo`, never a local boolean; `getPublicAccess` checks `websiteAccess` *first* so a configured base URL alone can never mark anything public (mutation-tested in 038); 037 already handles failed permission writes with a toast plus a persistent `Not saved` marker; and no code anywhere keys on a bucket's name. **The one real gap is #4**: the toggle is a bare `ToggleField` inside a 500 ms debounced auto-save, so one stray click makes a bucket world-readable with no prompt. Step 1 lifts `websiteAccess` out of the watch path and gates ON behind a confirm modal while leaving OFF immediate — confirming a *disable* would only train people to click through. **Three corrections to the brief**, all evidence-backed: (a) its Options B/C (prefix- and per-object-level) are **not implementable** — `backend/schema/bucket.go` has no ACL or policy field and Garage implements no bucket policies, so bucket-level is forced, not preferred; (b) revocation returns **404**, not 403 — measured against a live Garage, a non-served bucket answers `404 Not Found`, which is the non-existence-leaking behaviour the brief wanted and is already native; (c) the premise that showing a URL for a publicly-readable bucket is "too risky" is inverted — `websiteAccess: true` *is* the opt-in; the real risk (a base URL implying public) was closed by 038. Frontend-only; Garage already enforces everything. |
| [040](040-floating-upload-card.md) | Move the upload queue into a persistent floating card (Immich-style) | P2 | M | MED | 037+038 merged ✅ · stacked on **039** | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/040` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a215a63e688c1c213`, branch `advisor/040-floating-upload-card`, commit `6e873bb`, on `advisor/039-…` (`e024dc8`). **Reviewer re-verified**: typecheck 0, 12 files / **139 tests** / 1 pre-existing skip (exactly +10), build 0. Scope exactly the 8 in-scope files; `backend/` and `src/lib/website.ts` diffs **empty**; `grep upload-panel src/` → 0; `getBucketWebsiteObjectUrl` absent from `src/components/`; no `setTimeout` in the card (no auto-dismiss). Card mounted as a sibling of `<main>` in `main-layout.tsx`, so it survives route changes. **Mutation-tested twice**: restoring the full `cleanup()` on the error path fails the retry test (retry depends on the `File` surviving a failure); blanking the `Private` label fails the carry-forward test. **Carry-forward honoured** — the reviewer required 039's `Private` label to survive the panel's deletion, and it does, alongside `[Copy URL]`; an unknown bucket renders **neither** (fails closed). Three judgment calls, all accepted: (a) `Private` also shows for `public-no-url`, preserving the base panel's actual behaviour over a narrower reading of the plan's prose; (b) no animation plugin exists in this repo, so a small local `FadeIn` using core Tailwind utilities behind `motion-safe:` replaced the unavailable `tailwindcss-animate` classes — no new dependency; (c) `tailwind.config.js` left untouched because no component uses the `z-*` classes — every real usage sets `zIndex` inline from `Z_LAYERS`, which the card matches. **Known leftover**: `getUploadItemPublicUrl` in `upload-queue.ts` is now unused by production code (the card calls `getPublicAccess` directly to distinguish *unknown* from *private*); still exported and tested, flagged for a future cleanup. Live browser check **not** performed. (stamped `main`=`0edc65a`, stack tip `03ee680`). Repo-first investigation found four things that reshape the brief. **(1) A global upload manager already exists** — `upload-queue.ts`'s zustand store and its `handles`/`pendingFiles` Maps are **module-level**, so uploads already survive navigation untouched; only the *card* dies, because it is mounted at `browse-tab.tsx:72`. This is a **mount-point move**, not a state-management project, and no new dependency is warranted. **(2) Real byte progress already exists** (`xhr.upload.onprogress`), so the brief's §7 is already satisfied and no faked progress is needed. **(3) Cancellation is genuinely supported** (`xhr.abort()` via `UploadHandle`) so a Cancel button is real, not decorative. **(4) Retry is currently impossible** — the error path calls `cleanup(id)`, which deletes the `File` from `pendingFiles`; the plan's one transport change splits that into `releaseHandle`/`releaseFile` so a failure keeps its bytes. Also: **the reporting-cleanup the brief anticipates is already done** — the per-file success toast was removed when the queue landed, and the only toasts left in `actions.tsx` (20-file guard, folder-creation) are not upload status and must stay. **Hard-gated on the 037/038 stack merging**: the brief requires reusing the existing public-link helper, but `main`'s `getBucketWebsiteObjectUrl` does not encode the object key and still carries the path-style mode that 404s — so Phase 0 refuses to proceed unless `grep -c getPublicAccess src/lib/website.ts` ≥ 1. Adds a `uploadCard` entry to `Z_LAYERS` (below `toast`, above `dropdown`); no auto-dismiss timer at all. |

### Release & versioning

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|---|---|---|---|---|---|---|
| [041](041-version-and-update-check.md) | Let the app report its own version, and optionally tell you when a newer one exists | P3 | M | LOW | — | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/041` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a9d34fc2561fc2529`, branch `advisor/041-version-and-update-check`, commit `29b0574`, from `main` `bb57345`. **Reviewer re-verified**: Go gofmt/vet clean, `go test -race ./...` all 6 packages `ok` on pinned `golang:1.25.12`; frontend typecheck 0, 13 files / **144 tests** / 1 pre-existing skip, build 0. Injection proven end-to-end: `-ldflags "-X main.version=v9.9.9"` → binary prints `v9.9.9`; plain build → `dev`. `grep api.github.com src/` → 0 (browser never calls GitHub); no semver dependency added. **Mutation-tested both**: naive string compare in `isNewer` fails 3 subtests incl. the `3.9.0`/`3.10.0` trap; dropping `Version()`'s empty fallback fails `empty_falls_back_to_dev`. **⚠️ ADVISOR ERROR — the plan specified code that would not compile.** It said `backend/router/config.go` should call `Version()`, but `Version()` lives in `backend/version.go` which is `package main`, and Go forbids importing `package main`. The executor caught it and resolved it with the codebase's existing singleton-injection pattern: `var AppVersion` in `router/config.go`, pushed from `main.go:69` — verified to run *before* `HandleApiRouter()` (117) and `ListenAndServe` (136). **Second advisor error, also caught**: the Scope assigned the tag-vs-`package.json` guard to `ci.yml`, which triggers only on `push: branches:[main]` and `pull_request` — **never on tags** — so the guard would have been permanently dead code and could never have caught the `v3.2.0` drift that motivated it. Executor placed it in `release.yml` (the only tag-triggered workflow) instead. A third done-criterion miscounted: `main.version` appears in 3 files, not 4, because `docker-publish.yml` passes `VERSION` as a **build-arg** that `Dockerfile`'s `ARG` feeds into the ldflags — all four paths are wired, verified by reading the chain. `/update-check` confirmed on the **inner** (authenticated) router, so no unauthenticated caller can make the service emit outbound requests. Live checks **not** performed. |
| [044](044-guided-update-instructions.md) | Tell the operator how to update *this* deployment | P3 | S | LOW | — | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/044` merge commit is an ancestor of `main`).** _Was:_ DONE — awaiting merge.** Sonnet executor, branch `advisor/044-guided-update-instructions`, commit `ee0bcc4`, from `4b3d529`; **reviewer confirmed it still merges cleanly onto `main` after v3.5.1**. **Reviewer re-verified**: typecheck 0, **183 tests** / 1 pre-existing skip, build 0; Go gofmt/vet clean, all 6 packages `ok` on pinned `golang:1.25.12`. Scope exactly the 6 in-scope files; `backend/version.go`, `backend/middleware/` and `.github/` diffs **empty**; `isNewer`/`parseNumericVersion` untouched. **Security done-criteria hold**: `grep -nE "os/exec|syscall.Exec|os.Create|io.Copy|O_TRUNC|O_CREATE" backend/router/update.go` → **no matches** — nothing downloads, writes or executes. The probe is `os.OpenFile(path, os.O_WRONLY, 0)` followed by an immediate `Close()`, never truncating. **The subtle trap was handled correctly**: `utils.Cache.Set` runs *before* `Deployment`/`UpdateCommand` are assigned, so the cached value never carries a deployment snapshot, and the cached-read path re-sets them per request — all four paths (disabled, cached, check-failed, success) populate them. **Reviewer ran two mutations directly**: inverting the fail-safe (permission error → `binary`) fails `TestClassifyOpenResult`; dropping the fields from the disabled path fails `TestDeploymentFieldsAreSetWhenDisabled`. **⚠️ ADVISOR ERROR — amended after review (1 revision round), commit `4db344a`.** The plan mapped `managed` → `docker compose pull && docker compose up -d`. That is wrong for the most common *correct* deployment: `managed` means only "this process cannot write its own executable", which is equally true of a container **and** of a properly deployed systemd service (root-owned binary, non-root service user, or `ProtectSystem=strict` making the filesystem read-only to the process). The perverse consequence — the `binary` branch only fires when the service can overwrite its own executable, i.e. mostly when running as **root**, so the least safe configuration got the most useful advice and every hardened one got advice that was false. Surfaced by the maintainer asking whether the updater could run from the browser on a binary install. **Amendment**: `managed` now returns `""` and the About tab renders prose that is true either way — *"This deployment is updated from outside the app — replace the container image or the binary and restart the service."* — with no code block and no copy button. Guessing was refused for the same reason §0 refuses runtime sniffing for the *detection*; the guess had crept back in through the *command*. **Two regression guards**: `TestUpdateCommandFor/managed` asserts exact `""`, and `managed_never_suggests_docker` asserts the result contains no `docker` with a failure message stating the rationale; the frontend asserts the managed rendering has no `<code>`, no copy button, and no `docker` in `document.body.textContent`. **Reviewer re-verified after the revision**: typecheck 0, **185 tests** / 1 pre-existing skip, build 0, all Go packages `ok`; reintroducing the docker command fails **both** guards; `grep -rn docker src/ backend/router/update.go` shows nothing app-emittable (only a pre-existing, unrelated `docker exec garage` hint in `connect-node-dialog.tsx`). **Manual checks still NOT performed** — outstanding: a real binary run from a writable path shows the `systemctl`/`install` command; the Docker image (uid 65532 vs root-owned `/main`) shows the **managed prose**; a hardened systemd unit likewise shows the prose and never a docker line; the clipboard receives exactly the one-line string. (planned at `90a91fd`). Asked as "plan update mechanism"; investigation established that **self-update is impossible in one deployment mode and a liability in the other**, so the plan deliberately builds guided update instead and forbids drifting back. **Docker: the process cannot write its own binary** — `Dockerfile:74` copies `/main` with no `--chown` (root-owned) while `Dockerfile:84` sets `USER nonroot:nonroot`, on a distroless base with no shell or package manager. **Binary: technically possible, but it would be an RCE path** — this process holds the Garage admin token, and doing it safely needs published checksums and a signing key, **neither of which exists** (`release.yml` attaches raw binaries via `action-gh-release` with no integrity step). Maintainer chose guided update (2026-08-10). The detection is deliberately narrow: **not** "am I in Docker?" (unreliable across Docker/podman/Kubernetes/nerdctl) but **"is my own executable writable by me?"** — an `os.OpenFile(O_WRONLY)` probe that opens and immediately closes, never truncating, failing safe to `managed` on any doubt, because telling a container operator to overwrite a binary is worse advice than telling a binary operator to check their setup. Extends plan 041's `/update-check` with `deployment` + `updateCommand`, **set per request and never cached** so a redeploy isn't frozen behind the 6-hour TTL, and populated on all three response paths so the command shows even when checks are disabled. Done criteria assert **no** `os/exec`, `syscall.Exec`, `os.Create` or `O_TRUNC` anywhere in the file. |

### Operations

| Plan | Title | Status |
|---|---|---|
| [029](029-admin-recovery-cli.md) | Offline admin-recovery CLI (`-reset-password` / `-create-admin` / `-list-users`) | **DONE** — merged `bcbf86c`. **Independently verified**: `git diff --stat` over `backend/router/` + `backend/middleware/` is **empty** (no HTTP surface added); CLI smoke test leaked **0** hashes/passwords; passing a password as an argument is refused (`no password on stdin`); gates + `govulncheck` clean; x/term settled back to the MVS-selected v0.29.0 so `go.mod` gained exactly one line. |

> **Plan 029 carried a wrong done-criterion** (advisor error, caught by the executor): it required `grep -rn "reset-password" backend/router/ backend/middleware/` to be **empty**, but that pattern also matches the *pre-existing, authenticated* `POST /admin/users/{id}/reset-password` route shipped in plan 025. The executor proved it pre-existing with `git grep` at the base commit and a zero-diff `git diff --stat`, and correctly did not delete a working endpoint to satisfy a bad grep. The substance of the criterion — **no new HTTP surface** — held. Future plans should assert on the *diff*, not on a repo-wide grep.

### UI architecture

| Plan | Title | Status |
|---|---|---|
| [033](033-header-account-settings.md) | Move the username + Settings into the header's right corner | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/033` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a3a46b7105342233a`, branch `advisor/033-header-account-settings` (4 commits, tip `26fd772`), **stacked on `advisor/031-…`**. **Reviewer re-verified**: typecheck 0, `pnpm run test` 9 files / 78 passed / 1 pre-existing skip — including `menu.test.tsx` 11/11 untouched — and build 0. Scope exactly the four in-scope paths. Theme picker and `LogoutButton` confirmed surviving in the sidebar diff (only the `Settings` nav entry, the `Signed in as` `<p>`, its wrapper `<div>`, and the now-unused `Settings` icon import were removed). **Mutation-tested the load-bearing property**: adding `if (!username) return null;` to the chip fails `still renders a working Settings link before the username loads` — the test really does pin "never strand `/settings`", which matters because this chip is now its only entry point. Live browser check **not** performed. (stamped `f9cd2c6`) — the account cluster is stranded bottom-left of the sidebar (theme picker, 12 px "Signed in as …", Logout) while `Settings` is a fifth main-nav entry. Maintainer scoped it (2026-08-05) to **username + Settings only**: theme and Logout **stay** in the sidebar, and `Settings` **leaves** the nav list. Deliberately a plain `Link`, not a `<Menu>` — with one destination a dropdown costs a click to reach its only item. Consequence the plan pins with a test and a STOP condition: the header chip becomes the **sole** route to `/settings`, so it must render unconditionally, including while `useAuth().username` is still `undefined` on first paint. Frontend-only, 4 files. |
| [032](032-menu-hide-and-intrinsic-width.md) | Menu `hide()` + intrinsic width sizing | **DONE** — merged `dbfc2a0`, shipped in **3.1.0**. **Live-verified in-browser**: intrinsic width working (`width: max-content`, surface **174 px** sized to its longest item vs the old fixed `w-56` = 224 px; `min-width` 45.6 px = trigger width; `max-width` 1424 px = viewport − padding; all 4 items untruncated). Scroll-away turned out to be handled *more* strongly than specified — `useDismiss` **closes** the menu (`sameNodeStillMounted: false`, `aria-expanded: "false"`) before `hide()` needs to blank it, so `hide()` is the belt to that suspenders. The executor's jsdom stub had to fake `Element.prototype.clientWidth/clientHeight` as well as `getBoundingClientRect` — floating-ui measures clipping ancestors with the former, and jsdom answers 0, which made `hide()` mark every reference clipped and broke all 7 pre-existing menu tests. Mutation-tested. (stamped `b48f1ae`) — follow-up to 028. Verified against the merged code: portal/flip/shift/offset/size/Esc/outside-click/keyboard/ARIA/focus/z-index are **all already done**; only two items from the follow-up request are genuinely missing — `hide()` (so a menu disappears when its trigger scrolls out of view instead of floating over unrelated content, a side effect of portalling *out* of the clipping ancestor) and intrinsic width (fixed `w-56` at `users-tab.tsx:262` and `min-w-40` at `nodes-list.tsx:276` defeat "min = trigger width, grow to content, cap at viewport"). Tooltip / nested menus / scroll-lock / virtualized tables remain rejected — no callers. |
| [030](030-spa-cache-headers-stale-chunks.md) | Stale-chunk breakage after upgrade (asset 404s, cache headers, preload recovery) | **DONE** — merged `af09c73`, shipped in **3.1.0**. `isBuildAsset()` in `ui_prod.go` now 404s a missing hashed asset instead of answering it with `index.html` as `text/html` 200; `no-cache` on index, `immutable` on hashed assets; `src/lib/preload-recovery.ts` listens for `vite:preloadError`. **Plan step 3 self-contradicted** (advisor error): it specified a boolean "already reloaded" flag *and* "clear the flag on successful load", which guarantees the infinite reload loop the same plan forbade. The executor correctly used a **sessionStorage timestamp with a 10 s cooldown** instead. (stamped `4f9d4db`). Reproduced live: after upgrading the binary, an open tab requests a hashed chunk that no longer exists; `ui_prod.go` answers it with **`index.html` as `text/html` 200**, so the browser parses a document as a JS module → *"error loading dynamically imported module"*. Three fixes: 404 for missing build assets (fallback is for routes only), `no-cache` on `index.html` + `immutable` on hashed assets, and a guarded `vite:preloadError` self-reload. Sequenced after 028 — both touch the frontend. |

| [028](028-floating-overlay-menus.md) | Portal-based floating menus (fix dropdown clipping) | **DONE** — merged `c2645c5`. 7/7 menu tests incl. the portal-escapes-overflow assertion. **Live-verified in-browser** on the last row of the object table: `escapesOverflowAncestor: true`, rendered in a portal directly under `<body>`, `z-index: 1000` from `Z_LAYERS`, anchored 4 px below the trigger, `fullyInViewport: true` at 1440×900, `role="menu"` + `aria-expanded="true"`. All three legacy workarounds deleted (two manual flip calculations, the `.dropdown-content` z-index rule, the `z-10` patch, the `ActionItem` wrapper); no `Dropdown` import remains. Known limitation: floating-ui skips `aria-disabled` items during arrow-key navigation, so a blocked action's reason is hover-discoverable but not keyboard-discoverable (not a regression — the old `<a>` without `href` was not focusable either). (stamped `4f9d4db`) — `react-daisyui` `Dropdown` has no portal, so all 5 action menus are clipped by `overflow-*` table wrappers (`nodes-list` is `overflow-y-hidden`, guaranteed). The repo already carries **three workarounds for this one cause**: manual flip in `object-list.tsx:188`, another in `nodes-list.tsx:274`, and z-index patches in `styles.css:28` + `users-tab.tsx:290`. Fix: `@floating-ui/react` + `FloatingPortal`, one shared `Menu` primitive, central z-layer scale, all hacks deleted. |

**Considered and rejected — "Universal Object Storage Support"** (requested 2026-08-04, declined after recon).
The ask was a `StorageProvider` interface with 13 provider adapters (incl. native GCS/Azure), a connection manager with encrypted secrets, multi-tenant storage profiles, lifecycle/object-lock/KMS/versioning, and a resumable upload engine. Measured coupling: only `browse.go` (633 lines) is provider-agnostic, and even it calls Garage's admin API 7× because `getBucketCredentials` fetches per-bucket S3 keys **from Garage**. Dashboard, Cluster layout, Buckets (global aliases, Garage quotas, website config), Keys, Permissions and the catch-all proxy are 100% Garage admin API — they have no meaning against AWS/GCS/Azure. So "the rest of the application should never know which provider is used" is unachievable: most of the application *is* the Garage console. Maintainer chose to defer.
**Not planned — `file:///` security error** (reported 2026-08-06 alongside the upload/share issues, at commit `796039f`).
Firefox logged *"Security Error: Content at https://…/buckets/…?tab=browse&prefix=hp%2F may not load or link to file:///"*.
**Not attributable to this codebase.** Every URL the frontend constructs was
enumerated and checked: `window.open` (`object-actions.tsx:35`,
`object-list.tsx:79`), `<img src>` (`object-list.tsx:231`,
`sidebar.tsx:40`), all seven `href=` sites, both `window.location.href`
assignments (`api.ts:97`, `sidebar.tsx:109`), and `api.ts:69`'s
`new URL(API_URL + url, window.location.origin)`. All are either relative
`/api/…` paths or explicit `http(s)://` absolutes. There is no
`createObjectURL`, no drag-and-drop / `dataTransfer` handler, and no `file:`
literal anywhere in `src/`. Most likely a browser extension content script.
*If it recurs*: reproduce in a Firefox private window with extensions disabled;
if it survives that, expand the console entry's source frame — the filename
identifies whether it is app code or an extension — and reopen with that line.

*If revisited*: the tractable subset is **stored S3-compatible connections for the object browser only** — 11 of the 13 providers (AWS, MinIO, B2, R2, Wasabi, DO Spaces, Linode, Scaleway, Ceph, Garage, SeaweedFS) speak the same S3 API and need **one** adapter; only GCS and Azure need separate SDKs. Also rejected as speculative: building Combobox / Command Palette / Color Picker / Date Picker / Autocomplete primitives, which have zero callers in this codebase.

### Security follow-up

| Plan | Title | Status |
|---|---|---|
| [027](027-clear-govulncheck.md) | Clear `govulncheck` — exact Go toolchain pin + `aws-sdk-go-v2` bump | **DONE** — merged `41fb578`. Go pinned to exact `1.25.12` in all 4 places; s3 v1.59.0→**v1.106.4**, eventstream→v1.7.16. `govulncheck` → **No vulnerabilities found** (verified independently). **Zero source changes** — `browse.go` untouched. Live-verified vs real Garage: upload 200 + byte-identical `?dl=1` round-trip (30 000 random bytes), unauthenticated presigned fetch returns exact bytes, list/multipart/bulk-delete 200. Executor found `release.yml` was pinned to `"1.24"` — *below* go.mod's floor, so every release build silently downloaded a toolchain. |
| [043](043-object-serving-and-security-headers.md) | Stop serving object bodies as executable documents on the console's origin | **DONE — MERGED to `main` (verified at reconcile 2026-08-26, `6a36683`: `advisor/043` merge commit is an ancestor of `main`; feature present in tree).** _Was:_ DONE — awaiting merge.** Sonnet executor, worktree `.claude/worktrees/agent-a1c4b9eecdeeaf1e7`, branch `advisor/043-object-serving-and-security-headers`, commit `71bb1b5`, from `main` `8e0606d`. 12 files, +369/−15. **Reviewer verified independently** (the executor returned mid-gate, so I re-ran everything on its tree before it committed): Go gofmt/vet clean, `go test -race ./...` all 6 packages `ok`; frontend typecheck 0, **151 tests** / 15 files, build 0. Security properties confirmed by reading the committed code: `image/svg+xml` **absent** from the allowlist; `nosniff` on all three branches (view/thumbnail/download); `Content-Security-Policy: sandbox` on the view path; `middleware.SecurityHeaders(sessionMgr.LoadAndSave(mux))` at `main.go:130` — outermost; `csrf.go`/`auth.go`/`audit.go`/`ui_prod.go` **zero diff**. **The CSP prerequisite genuinely landed**: a real build shows `window.__BASE_PATH` → **0** occurrences in `dist/index.html`, replaced by `<meta name="app-base-path">`, so `script-src 'self'` is real rather than aspirational. **All three mutations caught**: adding `text/html` to the allowlist fails `html_is_never_inline-safe`; removing `frame-ancestors 'none'` fails `TestSecurityHeadersSetOnEveryResponse`; accepting the raw `%BASE_PATH%` placeholder fails `treats the unsubstituted placeholder as no base path`. Executor answered the `addBasePath` STOP condition properly — it **read** the regex `(href|src)=["'](/[^"'>]+)["']` and showed `name`/`content` cannot match, rather than merely not editing the file. Reviewer separately confirmed `?dl=1` still forces attachment after falling through the new block. **Live/browser checks NOT performed** — a CSP fails silently, so the manual list (zero console violations after login, PDF under `sandbox`, thumbnails, `.html`/`.svg` downloading) is the real acceptance test and remains outstanding. (stamped `8e0606d`, v3.4.1) — **the audit's only HIGH, shipped in v3.2.0/3.3.0/3.4.0/3.4.1.** `GetOneObject` copies an object's *stored* `Content-Type` verbatim and streams the body; `object-list.tsx:82` opens it `?view=1` **same-origin as the console**. Repo-wide greps for `nosniff`/CSP/`X-Frame-Options`/`Referrer-Policy` return **zero**. The type is caller-controlled (`PutObject` takes the multipart header, and anyone with S3 write access sets it directly through Garage), so a planted HTML-ish object executes script inside the console's origin — able to drive authenticated calls and read the deliberately non-`HttpOnly` `csrf_token`, behind which the catch-all proxy attaches the Garage **admin token**. Fix serves non-allowlisted types as attachments (**SVG deliberately excluded** — XML that carries `<script>`), adds `nosniff` + `Content-Security-Policy: sandbox` per object response, and adds a `SecurityHeaders` middleware as the outermost wrap. **Three prerequisites the investigation surfaced, each of which would otherwise break something:** (1) `CreateThumbnailImage` encodes **JPEG** while the handler declares `image/png` — harmless only because browsers sniff, which `nosniff` stops, so every thumbnail breaks unless fixed first; (2) `index.html` ships an **inline `<script>`** setting `window.__BASE_PATH` (verified present in `dist/`), which `script-src 'self'` blocks — moved to a meta tag; (3) a **direct** hit on `/index.html` bypasses `ui_prod.go`'s `%BASE_PATH%` substitution, so the literal placeholder can reach the browser — the new reader guards for it. `style-src` keeps `'unsafe-inline'` as a knowing trade: `@floating-ui` cannot position menus without inline style attributes, and no external resources exist anywhere (`default-src 'self'` verified viable). |

> **Checksum hazard worth remembering** (found by capturing real wire bytes, not assumed): the app dodges newer aws-sdk-go-v2 default checksum/trailer behaviour only because `getS3ClientForEndpoint` builds `aws.Config` as a **struct literal**, leaving `RequestChecksumCalculation` unset. Switching to `config.LoadDefaultConfig` would resolve it to `WhenSupported` and change the wire format Garage sees. Probed all three settings against Garage — it accepted each — but any future refactor there needs the live S3 matrix. — CI's `security` job is RED on `main`; v3.0.0 shipped with it red. Measured locally: Go 1.26.5 clears all six stdlib findings, leaving only `GO-2026-5764` (s3 ≥ v1.97.3 / eventstream ≥ v1.7.8). Two commits; the s3 jump (v1.59→v1.97) needs the live Garage matrix — checksum defaults can break uploads invisibly. |

### Authentication overhaul (plan set 022–026, `/improve plan`, stamped `b6101af`)

Replaces env-var authentication (`AUTH_USER_PASS`) with a persistent, in-app user
system. **Strictly sequential — each plan's SENTINEL requires the previous one merged.**

| Seq | Plan | Title | Status |
|---|---|---|---|
| 1 | [022](022-persistent-user-store.md) | SQLite user store + `AUTH_USER_PASS` migration + auth rewire | **DONE** — merged `9895c48`. All gates green incl. `CGO_ENABLED=0`. **Live-verified independently**: open mode gone (unauth → 401); import logs once; env changed to `intruder:` → 401 while original admin still 200 (DB authoritative); env removed → still works; fresh DB → `needsSetup:true`. Container runs non-root with DB owned by 65532. |
| 2 | [023](023-first-run-setup-wizard.md) | First-run `/setup` wizard | **DONE** — merged `646d83a`. Atomic `CreateFirstAdmin` (single tx over a 1-connection pool; 8-goroutine race test). **Live-verified**: fresh instance → `needsSetup:true`; wizard creates admin + auto-logs-in; **second call → 409**; creds survive restart; wrong pw 401; unauth `/api/buckets` *and* proxied admin call both 401 (no anonymous admin-token leak); no password/hash in any response or log. Also bumped CI Go → 1.25. |
| 3 | [024](024-session-hardening-password-change.md) | CSRF, session expiry, self-service password change | **DONE** — merged `b78b0c0`. Double-submit CSRF (exact-match exemptions, constant-time compare, judges the *incoming* cookie), `IdleTimeout`, Settings → Account. **Live-verified**: token issued JS-readable (64 hex); `/setup` exempt → 200 without header; change-password 403 without header, 403 with a wrong token, 200 with the right one; **proxied admin write 403 without a token**; old password then 401 / new 200; no passwords in logs. |
| 4 | [025](025-admin-user-management.md) | Admin user management API + Settings → Users | **DONE** — merged `d514c87`. Two-layer authz (`/admin/` denied to viewers *including GET*, plus `requireAdmin` per handler); last-admin/self lockout guards. **Live-verified**: viewer GET **and** POST `/admin/users` → 403 (leak closed); list body contains no `$2a$`/`password_hash`; delete-self / self-disable / self-demote as sole admin → 409 with the row unchanged; with 2 admins deleting the other → 200 and removed; admin password reset → viewer logs in with the new one; no passwords in logs. |
| 5 | [026](026-auth-documentation-screenshots.md) | Auth docs, migration guide, screenshots | **DONE** — merged `0840dad`. New `docs/authentication.md` (schema, API table, roles, sessions/CSRF, recovery, extension points) + `docs/UPGRADING.md` (breaking changes, both migration paths, the keep-env-vars-without-a-volume trap). README/CLAUDE.md/`.env.example`/`backend/.env.example` corrected; 3 new screenshots captured live at 2880×1800. Docs-only diff. |

**Set complete (022→026), all merged into local `main` at `0840dad`.**
Documented mismatches found while writing the docs, left as-is and reported rather
than papered over: error bodies are plain text (only success responses are JSON);
`POST /auth/login` returns 500 (not 400) on a malformed JSON body — inconsistent
with every other credential handler, worth a one-line follow-up; `POST /setup`
does not stamp `last_login`, so the first admin shows "never" until their first
real sign-in.

> ✅ **Push-safe as of `646d83a`** (022+023 merged): authentication is mandatory
> *and* the `/setup` wizard exists, so a fresh deployment can bootstrap itself.
> Until 023 landed this pair was deliberately unpushed — 022 alone leaves a
> deployment without `AUTH_USER_PASS` unable to log in at all. Keep that in mind
> if you ever cherry-pick these apart.

**Maintainer decisions (binding):**
- **Authentication becomes mandatory** — open (no-auth) mode is removed, no opt-out. This is a **breaking change** for deployments relying on network isolation.
- **Full internal rename already done**; module is `github.com/d7eeem/garage-webui-ng`.

**Verified constraints (do not re-litigate):**
- SQLite driver **must** be `modernc.org/sqlite` (pure Go) — test-built under `CGO_ENABLED=0`. `mattn/go-sqlite3` needs CGO and breaks the distroless-static release build.
- The runtime image runs as **uid 65532**; `/data` must be baked into the image with that ownership or the DB is unwritable on a named volume.
- The app becomes **stateful** — a persistent volume is now required.

**Note on base**: worktrees come up on stale `origin/main` (`ee420fb`); every
executor dispatch must `git checkout -B <branch> main` first to land on the
released local `main`. Fork/`origin` are not pushed per-feature during this phase.

## Dependency notes

- **003, 004, 005, 006 depend on 002** because each modifies code with no
  regression coverage today — 003 rewrites a destructive delete path, 004 edits
  error and concurrency paths, 006 changes URL construction on every object
  operation. 002 creates the Go and Vitest suites, the `typecheck` script, and
  the CI workflow those plans' verification gates call.
- **011 hard-depends on 003.** Bulk selection layered on a list that silently
  truncates at 1000 objects would reproduce the exact "reported success, deleted
  a fraction" bug 003 fixes. Plan 011's step 1 checks for 003 and stops if it is
  absent.
- **010 depends on 007** loosely — both touch session code, and 010's design
  assumes token renewal exists. 010 also reads 005's corrected `/auth/status`
  shape; if 005 has not landed, design against its output anyway.
- **001, 007, 008 have no dependencies** and are safe to run first or in
  parallel with 002.
- **003, 004, and 006 all edit `backend/router/browse.go`.** Each names the
  others' territory in its "Out of scope" section, but running them concurrently
  on the same branch will conflict. Sequence them: 003 → 004 → 006, or rebase
  between.

### Suggested sequencing

```
001 ─┐
008 ─┼─► (independent, land any time)
007 ─┘   └─► 010 (design)

002 ─┬─► 003 ─┬─► 011 (design)
     │        └─► 004 ─► 006
     └─► 005

009 (design) ─► independent
```

## Audit scope

**Audited by reading**, at `standard` effort, across all nine categories:
the Go backend (~1,100 LOC, all 18 files) and the React/TypeScript frontend
(~4,600 LOC, hooks/pages/lib in full, `components/ui/*` primitives skimmed),
plus `Dockerfile`, `docker-compose.yml`, `README.md`, and the build configs.

**Not audited:**

- **Nothing was executed.** Go, `pnpm`, and `node_modules` were all absent from
  the machine the audit ran on, and installing a toolchain would have mutated
  the environment. No build, typecheck, lint, `go vet`, or test run informed
  these findings — every one comes from reading code. Executors must run their
  plans' verification gates for real.
- **Dependency advisories.** `pnpm audit` could not run without a package
  manager. `pnpm-lock.yaml` and `backend/go.sum` were not checked against
  advisory databases. Worth a separate pass.
- **`src/components/ui/*` primitives** (button, input, select, checkbox, toggle,
  chips, code, form-control) — skimmed, not read line by line.
- **Garage API version compatibility.** The code targets the v2 admin API
  (`/v2/GetClusterStatus` etc.); behavior against older Garage versions was not
  assessed.
- **Runtime behavior of any kind** — no Garage instance was available. Several
  plans include manual verification steps that are explicitly skippable for this
  reason, and mark clearly which conclusions depend on observation.

## Findings discovered during execution

Added after the original audit, from things that only surfaced once the
toolchain was available.

- **`pnpm run lint` has been failing on `main` all along** — 55 problems across
  ~20 files: 40 × `@typescript-eslint/no-explicit-any`, 14 ×
  `react-hooks/exhaustive-deps`, 1 × `react-refresh/only-export-components`.
  Verified at `ee420fb` in a clean worktree, unrelated to any plan's changes.
  The original audit could not catch this — `node_modules` was absent and
  installing it was out of bounds.
  - **Consequence for plan 002**: its CI workflow runs `pnpm run lint` as a
    blocking step, so CI would be red on the first push. 002 must either mark
    that step non-blocking or drop it until the backlog is cleared.
  - The 14 `exhaustive-deps` warnings deserve their own look — some are
    plausibly real stale-closure bugs rather than noise (e.g. the effects in
    `src/components/layouts/main-layout.tsx` and
    `src/pages/buckets/manage/overview/overview-website-access.tsx`). Not yet
    triaged one by one.

## Finding from the self-hosted CI bring-up (2026-08-18, unplanned)

`TestResolveUploadContentType/empty_content-type_resolves_ico` failed on the
self-hosted runner because Go's `mime.TypeByExtension` resolves `.ico` from the
**system mime database** (`/etc/mime.types`), which minimal containers lack —
and `.ico` is not in Go's built-in fallback table. The runner got the
`media-types` package as a workaround, but the finding is bigger than CI: **the
production image is `scratch`-based and has no mime database either**, so plan
038's `.ico` fallback in `resolveUploadContentType` (browse.go) silently
resolves to nothing in the shipped Docker image — exactly the environment the
helper was added for. Proper fix (one line, needs a plan or a quick follow-up):
register the type deterministically at init, e.g.
`mime.AddExtensionType(".ico", "image/vnd.microsoft.icon")`, which also makes
the test environment-independent.

### v4 track (2026-08-27)

Maintainer decision: plans written from 2026-08-27 on target the next major
(**v4**), starting with 060 and 061 — picked from a feature comparison with
[`Noooste/garage-ui`](https://github.com/Noooste/garage-ui). **Considered and
rejected from that comparison**: bucket quotas UI — already shipped
(`src/pages/buckets/manage/overview/overview-quota.tsx`, debounced autosave of
`maxSize`/`maxObjects`); OIDC login + team→bucket-prefix bindings — explicitly
deferred by the maintainer ("I don't need OIDC right now"); their config
shape (`teams[].bindings[{bucket_prefixes, permissions}]`, presets, default
deny, admins through the same authorizer) is noted in `plans/design/010` as
the reference model if the scoped-operator role is ever revisited; Helm chart
— distribution only, no demand recorded.

## Reconcile log

### 2026-08-26 — reconcile at `6a36683` (v3.8.0 + Jenkins CI)

- **Verified MERGED** (rows updated above): 031, 033, 034, 035, 036, 037, 038, 039, 040, 041, 044 — all were "awaiting merge"; each `advisor/NNN` merge commit is an ancestor
  of `main` and the feature is present in the tree (`DownloadArchive`,
  `AccountButton`, `X-Frame-Options` object headers, `relsign`, …). Every plan
  001–055 is now merged. 049 still needs the maintainer keygen it always did.
- **Refreshed TODO**: 056 (gate on 055 now satisfied — executable), 057 (defect 3
  rewritten for the Jenkins move; see row).
- **Plan files 034–051 are not on `main`.** The index rows exist, but the files
  live only on `origin/plans` (commit `adda5bf`, "track the 034-051
  implementation plans"), which is not an ancestor of `main`. `git ls-files
  plans` on `main` shows 001–033 and 052–057 only. Merging `origin/plans` (or
  cherry-picking `adda5bf`) is a maintainer action; until then, anyone reading
  `plans/` on `main` sees rows for plans they cannot open.
- **Plan 058 written 2026-08-26** (Allow Key dialog silent submit) — from a maintainer bug report the same day.
- **Still-open unplanned findings** (no plan written yet):
  1. **`.ico` upload content type is empty in the shipped image** — recorded in
     the section above; still unfixed at `6a36683` (`grep -rn AddExtensionType
     backend/` → nothing; `browse.go:201` still relies on
     `mime.TypeByExtension`, which reads `/etc/mime.types` that the `scratch`
     image lacks). One-line fix + makes the test environment-independent. S/LOW.
  2. **`release.yml` (signed binaries on `v*` tags) is not ported to Jenkins**
     and targets `self-hosted` runners (`d2c7eeb`) that no longer exist per the
     CI handoff — a tag push queues forever. Either port it as a
     `when { tag 'v*' }` stage in the `Jenkinsfile` (signing key as a Jenkins
     credential) or move it back to GitHub-hosted. M/MED.
  3. **Go-pin lockstep is doc-enforced only** — 057's maintenance note suggests
     a `Jenkinsfile` stage that fails when `Dockerfile`, `release.yml` ×2,
     `backend/go.mod` and the agent's `go version` disagree. S/LOW, after 057.

## Dependency audit (run 2026-07-30, after `node_modules` became available)

The original audit could not run `pnpm audit` (no `node_modules`). Now closed:

- **[actionable] `@remix-run/router` XSS via open redirects (HIGH, runtime).**
  Installed 1.19.0 via `react-router-dom ^6.26.0`; ships in the browser bundle.
  Fix: bump `react-router-dom` → `^6.30.1` (pulls patched router `>=1.23.2`).
  → **[plan 012](012-bump-react-router-xss.md), DONE** — router now 1.23.3,
  advisory cleared; merged into `integration-check`; **PR #9** on the fork.
- **[noise] 1 critical + ~20 high in the build toolchain** (`shell-quote`,
  `rollup`, `vite`, `postcss`, `glob`, `minimatch`, `cross-spawn`, `lodash`, …) —
  transitive under Vite/Tailwind, not shipped in the runtime bundle. Low-signal
  for a self-hosted app; address with a routine `pnpm update`, not urgently.
- **[gap] Backend Go deps not vuln-scanned** — `govulncheck` not installed
  (install deferred as a toolchain mutation). Deps read as reasonably current;
  `nfnt/resize` is unmaintained but low-risk (pure in-memory image resize).

## Findings considered and rejected

Recorded so nobody re-audits them.

- **Goroutine loop-variable capture in `backend/router/buckets.go:29`** — looks
  like the classic Go closure bug, is not. `backend/go.mod` declares `go 1.23.0`
  and Go 1.22+ gives each loop iteration its own variable. The closure is
  correct. (Plan 004 *does* rewrite this function, but for unbounded concurrency
  and nondeterministic result ordering — not for capture.)
- **The catch-all admin proxy injecting the cluster admin token**
  (`backend/router/proxy.go:23`) — this is the product. It is a Garage admin UI;
  proxying admin calls with admin credentials is the design, not a defect. The
  real finding is that it is unauthenticated by default, which plan 001 step 5
  addresses with a startup warning.
- **The bcrypt hash in `backend/.env.example`** — a documented placeholder in an
  example file, not a live credential. Not a secret leak.
- **`path.Clean(r.URL.Path)[1:]` in `backend/ui/ui_prod.go:25`** — no path
  traversal. `path.Clean` normalizes `..` before `fs.Stat`, and `http.FS`
  rejects escapes independently. `path.Clean` also always returns at least one
  character, so the `[1:]` slice cannot panic.
- **`COPY --from=alpine` with no matching build stage in the `Dockerfile`** —
  valid Docker: `--from` accepts an image name, so it resolves to `alpine:latest`
  and works. Only an unpinned-base nit, noted in plan 008's maintenance section
  and deliberately left out of scope there.
- **`strings.Split(AUTH_USER_PASS, ":")` truncating a hash containing `:`**
  (`backend/router/auth.go:25`) — bcrypt's alphabet is `./A-Za-z0-9` plus `$`.
  A `:` cannot appear in a bcrypt hash. Safe as written.
- **`useRevertChanges` ignoring its `version` argument**
  (`src/pages/cluster/hooks.ts:75-80`, called with `data.version + 1` at
  `nodes-list.tsx:114`) — the argument is dead, but Garage v2's
  `RevertClusterLayout` takes no version, so the hook is right and the caller is
  merely passing something unused. Cosmetic; not worth a plan.
- **`readableBytes` capping at `TB`** (`src/lib/utils.ts:22`) — a petabyte-scale
  value renders `"1.0 undefined"`. Real but currently unreachable for this
  project's users, and plan 002 pins the behavior with a skipped test that
  documents the gap. Extend the `sizes` array when someone has a PB cluster.
- **`readableBytes` divider inconsistency in the cluster node row**
  (`nodes-list.tsx:237` uses `1000`, `:246` uses the `1024` default) — capacity
  in decimal and free space in binary, side by side. Cosmetic; flagged so the
  next reader knows it was seen.
- **`window.confirm` for destructive actions** (`object-actions.tsx:38`,
  `nodes-list.tsx:104`, `keys/page.tsx:40`) — unpolished but functional, and
  commit `d1ad6a0` shows the maintainer is already iterating on confirmation
  wording. Not a defect. Plan 011's Q3 revisits it only in the context of bulk
  operations, where a native confirm genuinely stops being adequate.
- **`&& data?.version != null` after `window.confirm`**
  (`nodes-list.tsx:110-116`, `:118-125`) — if the version is null the confirm
  still appears and then nothing happens. A minor UX wart; not worth a plan on
  its own.
